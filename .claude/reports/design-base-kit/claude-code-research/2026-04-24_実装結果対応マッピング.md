# 実装結果対応マッピング

**作成日**: 2026-04-27  
**目的**: 調査A〜G の推奨事項と現在の実装（base-dev-kit-for-cc main ブランチ）の対応を記録

---

## 現在の実装ファイル一覧

```
./
├── CLAUDE.md
├── CLAUDE.local.md.example
├── .env.example
├── .gitignore
├── .mcp.json
├── README.md
├── LICENSE
└── .claude/
    ├── settings.json
    ├── settings.local.json          （gitignore対象）
    ├── settings.local.json.example
    ├── agents/
    │   └── code-reviewer.md
    ├── output-styles/
    │   └── code-review.md
    ├── plans/
    │   └── base-kit-構成プラン.md  （gitignore対象）
    ├── reports/                     （gitignore対象）
    ├── rules/
    │   └── coding-standards.md
    ├── skills/
    │   ├── commit-and-pr/SKILL.md
    │   └── orchestrate/SKILL.md
    └── templates/
        └── skill-request/
            ├── skill-cc-response.md
            └── skill-request-form.md
```

---

## 調査 A: 公式ドキュメント機能インベントリ

| 推奨機能 | 推奨区分 | 現実装 | 状態 |
|---------|---------|--------|------|
| CLAUDE.md | 必須 | `./CLAUDE.md` | ✅ |
| settings.json | 必須 | `.claude/settings.json` | ✅ |
| セッション管理（`--continue`/`--resume`） | 必須 | Claude Code 標準機能（設定不要） | ✅ |
| CLI インターフェース | 必須 | Claude Code 標準機能 | ✅ |
| セキュリティ・サンドボックス | 必須 | CLAUDE.md に無効化ガイドとして言及、sandbox: false がデフォルト | ✅ |
| MCP サーバー連携 | 推奨 | `.mcp.json`（anthropic-docs / context7 / fetch / github） | ✅ |
| Hooks（自動化） | 推奨 | `.claude/settings.json` の `hooks.SessionStart` に `git status --short` | ✅ |
| Skills（知識・ワークフロー定義） | 推奨 | `.claude/skills/commit-and-pr/`・`orchestrate/` | ✅ |
| Sub-agents（コンテキスト管理） | 推奨 | `.claude/agents/code-reviewer.md` | ✅ |
| Plan Mode（複雑タスク） | 推奨 | Claude Code 標準機能（設定不要） | ✅ |
| Agent Teams | オプション（実験的） | CLAUDE.md に参考情報として言及 | ✅ |
| Plugin 化 | 特定用途向け | 不採用（個人用なので不要） | ✅（意図的不採用） |

---

## 調査 B: サブフォルダ構造ベストプラクティス

| 推奨事項 | 現実装 | 状態 |
|---------|--------|------|
| CLAUDE.md はプロジェクトルート `./CLAUDE.md` | `./CLAUDE.md` | ✅ |
| skills/ を使う（commands/ は避ける） | `.claude/skills/` | ✅ |
| rules/ で path-specific rules をサンプル提供 | `.claude/rules/coding-standards.md` | ✅ |
| agents/ に code-reviewer のサンプル | `.claude/agents/code-reviewer.md` | ✅ |
| orchestrator は agents/ ではなく Skill 化 | `.claude/skills/orchestrate/SKILL.md` | ✅（調査G 結論を反映） |
| settings.local.json は空または最小限（gitignore 登録） | `.claude/settings.local.json.example`・`.gitignore` | ✅ |
| agent-memory/ はデフォルトで作らない | 未作成 | ✅ |
| output-styles/ は今回除外 | `.claude/output-styles/code-review.md` が存在 | ⚠️ 方針と不一致（後回し論点）|

---

## 調査 C: Slash Commands・Sub-agents・Skills の設計パターン

| 推奨サンプル | 種類 | 現実装 | 状態 |
|------------|------|--------|------|
| `commit-and-pr` | Skill（Task 型） | `.claude/skills/commit-and-pr/SKILL.md` | ✅ |
| `code-reviewer` | Sub-agent | `.claude/agents/code-reviewer.md` | ✅ |
| `orchestrator` | Skill（調査G で修正） | `.claude/skills/orchestrate/SKILL.md` | ✅ |
| `coding-standards` | rules/ | `.claude/rules/coding-standards.md` | ✅ |

---

## 調査 D: Anthropic 公式リポジトリ横断調査

| 推奨パターン | 推奨度 | 現実装 | 状態 |
|------------|--------|--------|------|
| `.claude/settings.json` 基本構成 | 必須 | `.claude/settings.json` | ✅ |
| `.claude/agents/` ディレクトリの準備 | 推奨 | `.claude/agents/code-reviewer.md` | ✅ |
| Slash Commands / Skills テンプレート | 推奨 | `.claude/skills/`（skills で実装） | ✅ |
| PostToolUse フック（自動フォーマット） | 推奨 | SessionStart フックを採用（PostToolUse は言語依存のため不採用） | △（意図的変更）|
| Sub-agents テンプレート（code-reviewer） | 推奨 | `.claude/agents/code-reviewer.md` | ✅ |
| CLAUDE.md の標準テンプレート | 必須 | `./CLAUDE.md` | ✅ |
| `.claude/commands/` ディレクトリ | 必須（調査時点） | 不採用（skills/ に移行） | ✅（skills を優先） |

---

## 調査 E: 推奨設定の根拠裏取り

| 設定項目 | 採用判定 | 現実装 | 状態 |
|---------|---------|--------|------|
| permissions.deny（.env 等の保護） | 必須 | `.claude/settings.json` の `permissions.deny` | ✅ |
| permissions.allow（最小限） | 必須 | `git status/diff/log/branch`・`WebFetch` を allow | ✅ |
| CLAUDE.md 200 行以内 | 必須 | `./CLAUDE.md`（200 行以内） | ✅ |
| CLAUDE.md 配置（`.claude/` 配下推奨） | 必須 | プロジェクトルート `./CLAUDE.md`（調査B 推奨に従いルート優先） | ✅ |
| settings.json の 3 層運用 | 必須 | `.claude/settings.json`・`settings.local.json`・`settings.local.json.example` | ✅ |
| CLAUDE.local.md / settings.local.json の gitignore | 必須 | `.gitignore` に記載済み | ✅ |
| sub-agents サンプル | 推奨 | `.claude/agents/code-reviewer.md` | ✅ |
| skills サンプル | 推奨 | `.claude/skills/commit-and-pr/`・`orchestrate/` | ✅ |
| hooks サンプル | オプション | SessionStart に `git status --short` | ✅ |
| sandbox 有効化設定 | オプション | CLAUDE.md にデフォルト無効として言及・`/sandbox` コマンドで有効化可能と記載 | ✅ |
| plugin 化 | 不採用 | 不採用 | ✅ |

---

## 調査 F: Hooks クロスプラットフォーム実装

| 判断 | 内容 | 現実装 | 状態 |
|-----|------|--------|------|
| Hooks をベースキットに含める（条件付き） | OS 中立コマンドのみ | `.claude/settings.json` の `hooks.SessionStart`: `git status --short` | ✅ |

---

## 調査 G: Sub-agent 間オーケストレーション

| 判断 | 内容 | 現実装 | 状態 |
|-----|------|--------|------|
| Orchestrator は Skill として実装 | `disable-model-invocation: true`・`allowed-tools: Agent, Read, Grep, Bash` | `.claude/skills/orchestrate/SKILL.md` | ✅ |
| `.claude/agents/orchestrator.md` は作成しない | 公式仕様で Sub-agent から Sub-agent 呼び出し不可 | 未作成 | ✅ |

---

## 未実装・後回し論点

| 項目 | 内容 | 理由 |
|-----|------|------|
| output-styles/ の整理 | `code-review.md` が存在するが「今回除外」の方針 | 後回し論点として記録済み |
| PostToolUse フック | 言語依存のため SessionStart のみ採用 | 言語中立を優先 |
| `.claude/agents/` 内の orchestrator | Skill 化により不採用 | 調査G 結論を反映 |
| `WebFetch(domain:github.com)` の権限絞り込み | 計画書クロスレビューで「広すぎる」と指摘 | 後回し論点（現在は `github.com/anthropics` に変更済み） |
