# 個別評価詳細

各レポートの強み・弱み・指摘・引用検証メモを 8 ファル分まとめる。
指摘の重大度ラベル（CRITICAL / IMPORTANT / SUGGESTION / POSITIVE）の意味は `01_指摘事項一覧.md` 凡例参照。

---

## 1. 計画書クロスレビュー結果

**ファイル**: `2026-04-24_計画書クロスレビュー結果.md`（59 行）

### 概要評価

「Plan エージェント別インスタンス」による計画書 v2 のレビュー結果。重大な問題 3 件・改善提案 8 件・良かった点 5 件・ユーザー判断結果・次のアクションを構造化して記載。判定は「条件付き承認」。

### 強み

- **POSITIVE P-01**: 5 部構成（重大問題 / 改善提案 / 良かった点 / ユーザー判断 / 次のアクション）が再利用価値高
- 「条件付き承認」の判定が明確で、何が条件かも具体的（重大問題 3 件への対応）
- ユーザー判断結果を別表で保全しており、判断履歴の追跡が容易

### 弱み・指摘

- **IMPORTANT I-06**: 後続調査・実装との連結不在
  - ユーザー判断「Hooks: 追加調査後に最終判断」「Orchestrator: 追加調査後に判断」と書かれているが、その後の調査 F・G の結論・最終実装が本ファイル内で追跡できない
  - 末尾に「結果反映状況」節（1〜3 行）を追記すべき
- **SUGGESTION**: 「重大な問題」セクションの「対応」欄で「対応案 A=Skill 化、B=追加調査」と並列記載されている箇所があるが、最終的にどちらが採用されたかが本文書から読み取れない
  - → 結果的には B（追加調査）→ 調査G を経て案2（Skill 化）採用、と現実装と一致しているが、この経緯がレビュー文書側からはたどれない

### 引用・参照の検証

- 計画書 v2 への参照（`.claude/plans/base-kit-構成プラン.md`）は具体的だが、現在のリポジトリに当該ファイルが残っているかは未確認（gitignore 対象の可能性あり）

---

## 2. 調査A: 公式ドキュメント機能インベントリ

**ファイル**: `2026-04-24_調査A_公式ドキュメント機能インベントリ.md`（96 行）

### 概要評価

Claude Code 公式ドキュメント 17 ページの網羅的取得結果と機能分類。「必須 / 推奨 / オプション / 特定用途」の 4 区分で組み込み判定を提示。

### 強み

- **POSITIVE P-02**: 4 区分による分類で、ベースキットへの組み込み判定が透明
- 「重要な最新情報」セクション（51〜54 行目）で commands→skills 統合・Auto Memory・Path-specific rules・Plan Mode の 4 つを列挙し、最新仕様の押さえどころを明確化
- ベースキット設計への含意（90〜95 行目）が「最小必須セット」「推奨セット」「動作する実用サンプル」「言語中立」の 4 観点で整理されており、後続作業の起点として実用的

### 弱み・指摘

- **CRITICAL C-01**: 取得成功率の数値不整合（15/17 と記載されているが、表は 15 行・✓ 14・`-` 1）
  - 実カウントから「14/15 (93.3%)」または表に欠落 2 行を補完して 15/17
- **SUGGESTION**: 「Auto Memory: 200 行上限」（53 行目）の「200 行」は何の上限か（インデックスの MEMORY.md か、各メモリファイルか、ディレクトリ全体か）の明示が無い
  - 公式の Auto Memory 仕様に照らして要再確認
- **SUGGESTION**: 「次の調査へ引き継ぐ確認事項」（85〜88 行目）はあるが、次調査側でどう処理されたかの追跡は無し

### 引用・参照の検証

- URL 列（17〜31 行目）は code.claude.com/docs/en/* で全部統一されており、見やすい
- ただし「slash-commands」（22 行目）の `-` は「skills に統合された」の意ではあるが、公式ドキュメント上は実際にリダイレクトされるのかページが存在しないのか曖昧

---

## 3. 調査B: サブフォルダ構造ベストプラクティス

**ファイル**: `2026-04-24_調査B_サブフォルダ構造ベストプラクティス.md`（127 行）

### 概要評価

`.claude/` 配下の公式サポートサブフォルダを網羅し、命名規則・避けるべきパターン・ベースキット推奨事項を提示。

### 強み

- 「公式サポートされるサブフォルダ一覧」（11〜21 行目）が表形式で網羅的
- 「避けるべきパターン」（103〜109 行目）で skill と command の併用、CLAUDE.md の重複配置などの典型ミスを列挙
- 「ベースキットへの推奨事項」（111〜119 行目）の 7 項目は現実装方針と一致（CLAUDE.md は project root、skills/ 採用、commands/ 不採用、output-styles 除外、agent-memory/ デフォルト不作成 etc.）

### 弱み・指摘

- **CRITICAL C-02**: フル機能構成図に「hooks/」記載（88 行目）
  - hooks は `.claude/settings.json` 内の JSON プロパティ。単独サブフォルダではない
  - 11〜21 行目の「公式サポートされるサブフォルダ一覧」表自体には hooks/ が含まれていないため、自己矛盾している
  - 修正: 88 行目から「`hooks/`」を削除、必要なら「+ settings.json の hooks プロパティ」と注記
- **SUGGESTION**: 「workspace/」「templates/」「reports/」などのカスタム（公式サポート外）ディレクトリへの言及が無い
  - 本レポート時点では未存在の論点だったため記載が無いのは妥当だが、後続の運用で追加された場合に「どこに線引きするか」のガイドが本レポートには無い
  - → 次回更新時に「カスタムディレクトリは `.claude/` 配下に置いてもよいが、公式機能との衝突回避のため命名に注意」を追記すべき
- **SUGGESTION**: 「avg-memory/ はデフォルトで作らない（118 行目）」と表内「`<agent-name>/MEMORY.md`」（20 行目）の関係が読みにくい
  - 「sub-agent が `memory:` フィールドを設定した場合に自動生成される」と明示すべき

### 引用・参照の検証

- 参考資料 4 件（121〜126 行目）は公式 URL のみで、本文中の引用箇所が「どの URL の何節」かが追跡できない
  - **SUGGESTION S-01**: 本レポート以降は引用元を「URL + 節タイトル + 引用文」で記述

---

## 4. 調査C: Slash Commands・Sub-agents・Skills の設計パターン

**ファイル**: `2026-04-24_調査C_CommandsAgentsSkills設計パターン.md`（318 行）

### 概要評価

3 機能（Slash Commands / Sub-agents / Skills）の frontmatter 仕様・実例分析・設計判断フロー・テンプレートを提示。レポート群の中で最も大容量・実装直結。

### 強み

- **POSITIVE P-03**: 「frontmatter 仕様 → 実例分析 → 設計判断フロー → テンプレート」の 4 段構成は学習効率が高い
- 公式リポジトリの実例（`commit-push-pr.md`、`notebook-review.md`、`code-reviewer.md` など）を引用付きで分析しており、抽象論ではなく具体例ベース
- 「8. 設計判断フロー」（210〜226 行目）の Yes/No 分岐ツリーは実装着手時の判断補助として有用

### 弱み・指摘

- ~~**CRITICAL C-03**~~: **✅ 取り消し（Opus 誤判定）**
  - 2026-04-27 に `https://code.claude.com/docs/en/skills` の frontmatter 参照表を WebFetch で確認した結果、指摘の 4 プロパティはすべて skills frontmatter で正式サポートされていることが判明:
    - `paths`: 「Glob patterns that limit when this skill is activated」として明記
    - `shell`: 「Shell to use for `` !`command` `` blocks」として明記（`bash` / `powershell` を受け付ける）
    - `effort`: 「Effort level when this skill is active. Overrides the session effort level.」として明記
    - `arguments`: 「Named positional arguments for `$name` substitution」として明記
  - 調査 C の元ファイルは正確。修正不要。
- **SUGGESTION S-04**: 「7. description の書き方」（189〜207 行目）で「動詞で始めない」とルール化しつつ、「10. テンプレート」の Template A（242 行目）で `[Action] [what]` と動詞始め例示があり矛盾
  - Template A の例示を「`<目的>を達成する。<トリガー条件> の場合に使用する。`」のような名詞句中心に修正
- **SUGGESTION**: 「11. 重要な注意点」（304〜309 行目）の「SKILL.md は 500 行以下推奨」「Agent system prompt は 2000 行以下推奨」の数値は引用元 URL が明示されていない
  - 「公式 `/skills` の Length 節 より」のような注記を追加

### 引用・参照の検証

- 「取得した実例ファイル」（11〜15 行目）でファイルパスは記載されているが、対応する GitHub URL が無いため再検証時の追跡コストが高い
- 「参考資料」（311〜317 行目）は本文中の引用と必ずしも一対一対応していない（特に GitHub の `.claude/` ディレクトリ参照）

---

## 5. 調査D: Anthropic 公式リポジトリ横断調査

**ファイル**: `2026-04-24_調査D_Anthropic公式リポジトリ横断調査.md`（183 行）

### 概要評価

Anthropic 組織の公開リポジトリ 8 件を `.claude/` 有無で分類し、`.claude/` を持つ 3 件（claude-code、anthropic-cookbook、claude-code-action）の構成を比較分析。

### 強み

- 8 リポジトリの一覧表（13〜22 行目）で対象範囲を明確化
- パターン抽出（106〜127 行目）が定量的（commands 100%、agents 67%、skills 33% など）
- 「`.claude/` を持たないリポジトリの特徴」（129〜135 行目）で「なぜ持たないか」の推測まで含めており、調査の深さがある

### 弱み・指摘

- **IMPORTANT I-05**: 「`agents/(空または最小限)`」「`commands/(空または最小限)`」（78〜83 行目）の表記が不正確
  - git は空ディレクトリを管理しないため、リポジトリ上に空ディレクトリは存在し得ない
  - 確認すべきは:
    - そもそもディレクトリ自体が存在するか
    - `.gitkeep` のみが配置されているか
    - 実ファイルが配置されているか（その場合は内容を取得すべき）
  - 修正: 実態を再確認し、「ディレクトリ存在確認は未完了（GitHub Web UI 経由では一部のみ確認可能）」のように曖昧さを明示
- **SUGGESTION S-03**: 「補足」セクション（177〜182 行目）で「GitHub Web UI 経由では取得困難」と認めているが、代替手段（mcp__github / WebFetch with raw.githubusercontent.com 等）を即時試した形跡が無い
  - 次回以降は「取得困難」と認識した時点で代替を 1 度試す運用に
- **SUGGESTION**: 「パターン3: Hooks の段階的採用 — 1/3 リポジトリ（確認済み）」（116〜119 行目）で「他リポジトリは settings.json が取得困難で未確認」とあるが、未確認のものをパターンとして提示するのは控えるべき

### 引用・参照の検証

- 各リポジトリの GitHub URL は本文に明示されていない（推測可能な範囲）
- claude-code-action の settings.json 内容（86〜102 行目）はインライン引用しており追跡可能

---

## 6. 調査E: 推奨設定の根拠裏取り

**ファイル**: `2026-04-24_調査E_推奨設定の根拠裏取り.md`（209 行）

### 概要評価

ベースキットで採用予定の 12 項目について、公式ドキュメントから引用付きで根拠を整理。各項目に「裏取り充足度」を併記し、最終的に「採用判定」表で意思決定。

### 強み

- **POSITIVE P-04**: 「裏取り充足度」と「採用判定」を別表で示す二段階評価が明快
  - 162〜177 行目: 裏取り充足度サマリ（強い 11 / 中程度 1）
  - 179〜193 行目: ベースキット採用推奨判定（必須 / 推奨 / オプション / 不採用）
- 全 12 項目に引用文が付与されており、後続検証の起点として優秀
- 項目12「プラグイン vs カスタム設定」（148〜160 行目）の引用「Start with standalone configuration in `.claude/` for quick iteration, then convert to a plugin when you're ready to share.」は本ベースキットの位置づけ判断（standalone）の決定根拠として的確

### 弱み・指摘

- **IMPORTANT I-01**: 項目11 sandbox の URL（141 行目）が `https://code.claude.com/docs/en/sandboxing` だが、公式に存在するか未確認
  - WebFetch で確認し、リダイレクトが発生していたら正しい URL に置換
- **IMPORTANT I-02**: 項目7 hooks の引用元（93 行目）が `https://code.claude.com/docs/en/hooks-guide` だが、調査A では `/hooks` と表記されており、`/hooks-guide` が別ページか同一かが不明
  - WebFetch で確認・修正
- **SUGGESTION**: 項目7「裏取り充足度: 中程度（詳細な use case list は full reference 参照が必要）」（98 行目）は誠実な記載だが、本レポート内で「full reference」へのリンクが無いため、後続作業者が追加確認を行う際の手がかりが不足
- **SUGGESTION**: 「未確定事項」（203〜207 行目）の 2 項目（hooks のクロスプラットフォーム実装方針、sandbox のデフォルト挙動）は調査F・現実装で結論が出ているはずだが、本レポートには反映されていない（実装結果対応マッピング不在の典型例）

### 引用・参照の検証

- 引用文が「>」記号で囲まれており可読性が高い
- ただし引用元 URL は記載されているが、ページ内のどの節からの引用かが省略されている
  - **SUGGESTION S-01**: 引用は「URL + 節タイトル + 引用文」の 3 点セットを標準化

---

## 7. 調査F: Hooks のクロスプラットフォーム実装可能性

**ファイル**: `2026-04-24_調査F_Hooksクロスプラットフォーム実装.md`（78 行）

### 概要評価

計画書クロスレビューで保留扱いになった「Hooks 採否」を最終判断するための追加調査。3 案を比較評価し、「OS 中立コマンドのみで実装」を条件付き採用と結論。

### 強み

- 短文ながら「仕様確認 → 公式実装パターン → 案の評価 → 結論 → 推奨サンプル」の構成が論理的
- 公式 claude-code-action の実装（17〜30 行目）を引用しており、根拠が具体的
- 結論部の「推奨サンプル」（51〜66 行目）が実装可能な JSON で示されており、現実装の `.claude/settings.json` の hooks 部（27〜37 行目）と一致
- 結果として「Hooks をベースキットに含める」判断が現実装に正しく反映済み（裏取りまで含めて筋が通っている）

### 弱み・指摘

- **IMPORTANT I-07**: `shell` プロパティの位置づけが曖昧（13 行目）
  - 「デフォルト shell: bash（`"shell": "powershell"` で PowerShell 指定可能）」と書いているが、これは hooks エントリのプロパティか frontmatter プロパティかの明示が無い
  - 公式 `/hooks` を再取得し、`shell` プロパティの正確な適用箇所と sample を本レポートに追記
- **SUGGESTION**: 「クロスプラットフォーム実装」と銘打ちつつ、推奨サンプル（53〜65 行目）が `git status --short` 1 件のみで「学習価値が高い」を主張しているのはやや弱い
  - 採否判断としては妥当だが、「学習価値が高い」の根拠（具体的にどんな拡張ができるか）が示されていない
- **SUGGESTION**: 案2「OS 判定分岐」の評価（38 行目）で「Windows で不確実 / WSL/Git Bash 前提」とあるが、Windows ネイティブ PowerShell を `shell: powershell` で使う案が評価表に含まれていない
  - もし `shell: powershell` が公式サポートなら、案 4 として追加検討の余地あり

### 引用・参照の検証

- 参考資料 3 件（73〜77 行目）は URL のみ。本文の引用箇所との対応は読み取れる範囲

---

## 8. 調査G: Sub-agent 間オーケストレーション機構

**ファイル**: `2026-04-24_調査G_SubAgent間オーケストレーション.md`（83 行）

### 概要評価

計画書クロスレビューで保留扱いになった「Orchestrator 実現可能性」を最終判断するための追加調査。「Subagents cannot spawn other subagents」という公式仕様を発見し、Skill 化を結論。

### 強み

- **POSITIVE P-05**: 「公式仕様の決定的引用 → 代替案評価 → 最終判断」の流れが説得力高い
- 12 行目の引用「If your workflow requires nested delegation, use Skills or chain subagents from the main conversation.」は、Orchestrator を Skill 化する判断の決定打
- 結論「`.claude/skills/orchestrate/SKILL.md` に実装」は現実装と一致
- 結論部で「`.claude/agents/orchestrator.md` は作成しない」と明記しており、誤実装の予防まで踏み込んでいる

### 弱み・指摘

- **IMPORTANT I-04**: 行番号参照（10 行目「Sub-agents ページ 714 行目」）は将来のドキュメント更新で陳腐化
  - 引用文 + 節タイトル（例: 「Subagents and parallel execution」）に置き換える
- **IMPORTANT I-03**: Agent Teams 制限の引用元未明示（36〜45 行目）
  - 制限事項（resumption 非対応、status lag、shutdown 遅延 など）を箇条書きで列挙しているが、引用元 URL・節名が記載されていない
  - 公式ドキュメント上の出典を追加（`/agent-teams` の Limitations 節 などが想定される）
- **SUGGESTION**: 「実装方針」（62〜76 行目）の YAML 例で `allowed-tools: Agent, Read, Grep, Bash` と書いているが、`Agent` ツールの正式な記法（`Agent(<subagent_type>)` のような細粒度指定が可能か）が本レポートに記載されていない
  - 現実装の orchestrate/SKILL.md は `Agent, Read, Grep, Bash` 表記を採用しているが、より細粒度な許可指定が公式サポートされていれば改善余地あり

### 引用・参照の検証

- 参考資料 2 件（80〜83 行目）はやや少ない
- Agent Teams の制限の出典が不明なまま箇条書きされている点が最大の弱点

---

## 共通観察

### 全レポートに通底する強み

1. 構成の一貫性（概要 → 主要発見 → ベースキット設計への含意）
2. 採用判定の明確化（必須 / 推奨 / オプション / 不採用）
3. 公式ドキュメントベースの裏取り意識

### 全レポートに通底する弱み

1. 引用の追跡性不足（URL のみ・節タイトル省略）
2. 「未確認項目」「次の調査への引き継ぎ」セクションの欠落（→ S-02）
3. 取得失敗時の事後追加取得が無い（→ S-03）
4. 実装結果との対応マッピング不在（→ I-08, S-05）

これらの共通弱みは個別の修正に加えて、調査レポートテンプレート自体の改善が望ましい。
