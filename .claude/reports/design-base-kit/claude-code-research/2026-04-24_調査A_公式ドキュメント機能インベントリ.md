# 調査A: Claude Code 公式ドキュメント機能インベントリ

## 調査概要

**調査目的:** Claude Code ベースキット構築のための公式ドキュメント全機能の網羅的な把握

**調査対象:** Claude Code 公式ドキュメント（https://code.claude.com/docs/en/）の15ページ

**実施日:** 2026-04-24

**調査方法:** WebFetch による各ドキュメントページの取得・分析（Explore エージェント経由）

## 取得したページ一覧

| URL | タイトル | 取得成否 |
|---|---|---|
| https://code.claude.com/docs/en/overview | Claude Code overview | ✓ |
| https://code.claude.com/docs/en/quickstart | Quickstart | ✓ |
| https://code.claude.com/docs/en/memory | How Claude remembers your project | ✓ |
| https://code.claude.com/docs/en/settings | Claude Code Settings | ✓ |
| https://code.claude.com/docs/en/hooks | Hook Types, Examples, and Use Cases | ✓ |
| https://code.claude.com/docs/en/slash-commands | （skills に統合済み - 独立ページなし） | 対象外 |
| https://code.claude.com/docs/en/sub-agents | Create custom subagents | ✓ |
| https://code.claude.com/docs/en/skills | Extend Claude with skills | ✓ |
| https://code.claude.com/docs/en/mcp | Connect Claude Code to tools via MCP | ✓ |
| https://code.claude.com/docs/en/plugins | Create plugins | ✓ |
| https://code.claude.com/docs/en/security | Security | ✓ |
| https://code.claude.com/docs/en/cli-reference | CLI reference | ✓ |
| https://code.claude.com/docs/en/ide-integrations | Use Claude Code in VS Code | ✓ |
| https://code.claude.com/docs/en/best-practices | Best Practices for Claude Code | ✓ |
| https://code.claude.com/docs/en/costs | Manage costs effectively | ✓ |

**取得成功率:** 14/15 (93.3%)（slash-commands は skills に統合されたため独立ページが存在せず取得対象外）

## 主要所見サマリ（ベースキット設計に直結）

### 必須機能
1. **CLAUDE.md** — `./CLAUDE.md` または `./.claude/CLAUDE.md` に配置。200 行以内 target。
2. **settings.json（4 層スコープ）** — Managed > CLI > Local > Project > User の優先度
3. **権限管理（permissions）** — allow / deny / ask ルール。`Tool(specifier)` パターンマッチ
4. **セッション管理** — `--continue` / `--resume` で再開可能

### 推奨機能
5. **Hooks** — 14 種類のイベント（SessionStart / PreToolUse / PostToolUse / Stop / UserPromptSubmit など）
6. **Skills** — `.claude/skills/<name>/SKILL.md`。オンデマンド読み込み（context 効率）
7. **Sub-agents** — `.claude/agents/<name>.md`。独立コンテキスト・専用権限
8. **MCP** — 外部ツール連携。`defer_tool_definitions: true` でコンテキスト節約
9. **Sandbox** — filesystem + network 隔離。approval fatigue 対策

### 重要な最新情報
- **Custom commands は skills に統合された**: `.claude/commands/X.md` と `.claude/skills/X/SKILL.md` は等価
- **Auto Memory**: `~/.claude/projects/<project>/memory/` に自動蓄積（200 行上限）
- **Path-specific rules**: `.claude/rules/*.md` に YAML frontmatter `paths:` で範囲指定
- **Plan Mode**: 実装前の計画レビュー機能（Shift+Tab で切替）

## 機能分類（ベースキット組み込み判定）

### 必須（Essential）— 6 項目
- Core Execution Environment
- 権限管理システム
- CLAUDE.md
- セッション管理
- CLI インターフェース
- セキュリティ・サンドボックス

### 推奨（Recommended）— 主要
- MCP サーバー連携
- Hooks（自動化）
- Skills（知識・ワークフロー定義）
- Sub-agents（コンテキスト管理）
- Plan Mode（複雑タスク）
- トークン追跡・コスト最適化

### オプション（Optional）
- LSP プラグイン（言語依存）
- Slack / Chrome 統合（特定用途）
- Desktop App / Remote Control

### 特定用途向け（Domain-Specific）
- Agent Teams（実験的）
- Plugin Marketplace（チーム共有用）
- Managed Settings（組織ポリシー）

## 次の調査へ引き継ぐ確認事項

1. **`.claude/` 配下の公式推奨サブフォルダ構造** → 調査B で深掘り
2. **Slash Commands / Sub-agents / Skills の具体的な設計パターン** → 調査C で深掘り
3. **各推奨設定の根拠確認** → 調査E で完了（引用付きで確認済み）

## ベースキット設計への含意

- **最小必須セット**: CLAUDE.md + settings.json + .gitignore
- **推奨セット**: 上記 + hooks + skills + agents + MCP
- **「動作する実用サンプル」を作るなら**: skills と agents の両方でサンプルを用意すべき
- **言語中立にするなら**: コマンド allow list を最小限に、skills/agents の内容を言語に依存させない
