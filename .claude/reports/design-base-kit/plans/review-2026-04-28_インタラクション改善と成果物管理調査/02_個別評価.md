# 個別評価詳細

レビュー対象 1 ファイル（`2026-04-28_インタラクション改善と成果物管理調査.md`、Sonnet 4.6 作成）の各セクション（A/B/C/D + 事項(2)）について、強み・弱み・指摘・引用検証メモを順にまとめる。
指摘の重大度ラベル（CRITICAL / IMPORTANT / SUGGESTION / POSITIVE / `[未確認]`）の意味は `01_指摘事項一覧.md` 凡例参照。

---

## 1. 事項(1) A. メッセージングアプリ経由のインタラクション

**ファイル該当範囲**: 23〜43 行目

### 概要評価

Discord/Telegram/iMessage の Anthropic 公式外部プラグインを、対応環境・セットアップ概要・出典ファイルを 3 列で整理して提示。「複数行ペースト」「長文の推敲」「誤送信ゼロ」「ファイル添付」の 4 観点で解決される問題を明示。実装の仕組み（MCP サーバー経由）と Windows 環境での推奨も記述。

### 強み

- **POSITIVE P-01**: 公式ドキュメント単独調査では到達しにくい「メッセージングアプリ経由」という抜本解決策を、`analyze-claude-code-doc-repos` インデックス経由で発見できている
- 4 観点の解決マッピング（35〜38 行目）が読者の意思決定を助ける
- Windows 環境向けの具体的推奨（42 行目）あり

### 弱み・指摘

- **CRITICAL C-01**: 引用ファイル名 `PLUGIN.md` が**実体不在**。実際は `README.md`。ディレクトリ列挙で確認済み
- **CRITICAL C-02**: Bun ランタイム前提（Discord/Telegram）が表に載っていない。Windows 推奨判断の根拠不足
- **IMPORTANT I-03**: Telegram に `download_attachment` ツールが**存在しない**にもかかわらず、Discord と同列に「download_attachment ツール経由」と記述（38 行目）
- **IMPORTANT I-05**: Discord セットアップの欠落ステップ（Message Content Intent / Bot サーバー招待 / `/reload-plugins` / `allowlist` 切替）

### 引用・参照の検証

| 引用先 | 検証結果 |
|---|---|
| `external_plugins/discord/PLUGIN.md` | ❌ 不在（実体は `README.md`） |
| `external_plugins/telegram/PLUGIN.md` | ❌ 不在（実体は `README.md`） |
| `external_plugins/imessage/PLUGIN.md` | ❌ 不在（実体は `README.md`） |
| Discord README.md（実体） | ✅ Quick Setup 8 ステップ・Tools 表（reply/react/edit_message/fetch_messages/download_attachment）を確認 |
| Telegram README.md（実体） | ✅ Quick Setup 6 ステップ・Tools 表（reply/react/edit_message のみ。download_attachment 無し） |
| iMessage README.md（実体） | ✅ Quick setup 5 ステップ・macOS 専用・Tools 表（reply/chat_messages） |

---

## 2. 事項(1) B. CLI 内での複数行入力改善

**ファイル該当範囲**: 46〜70 行目

### 概要評価

CLI 利用時の即時対応手段として 6 行の問題-解決-操作-出典マッピング表を提示。`/terminal-setup` による Shift+Enter 対応、`Ctrl+G` による外部エディタ統合、Vim キーバインド、`~/.claude/keybindings.json` カスタマイズに段階的に言及。

### 強み

- 「設定不要〜最小設定で即時対応」という入口の明示が良い
- `Ctrl+G`（外部エディタ統合）を「最実用的」と位置付けた評価が、長文・コードを含む指示の推敲を実践する場面で有用
- キーバインドカスタマイズへの導線あり

### 弱み・指摘

- **CRITICAL C-03**: `interactive-mode.md` に「Chat actions」セクションは**存在しない**（`Ctrl+S` の正典は `keybindings.md`）
- **IMPORTANT I-01**: `terminal-config.md` の正しいセクション名は「Enter multiline prompts」（"Multiline input" は `interactive-mode.md` のセクション）。複数 URL 引用で節タイトルを取り違えている
- **IMPORTANT I-02**: Shift+Enter ネイティブ対応端末（Ghostty/WezTerm/Warp/iTerm2/Kitty/Apple Terminal）は設定不要。「`/terminal-setup` 実行後」グループに巻き込む書き方が誤誘導

### 引用・参照の検証

| 引用 URL | 実在 | セクション存在 |
|---|---|---|
| `terminal-config.md` | ✅ | "Enter multiline prompts" ✅ / "Multiline input" ❌（別ページ） / "Paste large content" ✅ / "Edit prompts with Vim keybindings" ✅ |
| `interactive-mode.md` | ✅ | "General controls" ✅ / "Chat actions" ❌（`keybindings.md` 側） / "Multiline input" ✅ |
| `common-workflows.md` | ✅ | 正確なセクション名は "Reference files and directories"（"Reference files" は省略形） |
| `keybindings.md` | ✅ | "Chat actions" ✅（`chat:stash | Ctrl+S`） |

### POSITIVE

- 引用ごとに節タイトルを併記する姿勢自体は前進（前回レビュー B5 への対応の方向性として正しい）。次は節アンカー（`#section-id`）まで付与すれば再検証コストが下がる（S-03 参照）

---

## 3. 事項(1) C. IDE 統合 / Desktop アプリ

**ファイル該当範囲**: 74〜84 行目

### 概要評価

Claude Desktop / VS Code 拡張 / JetBrains 拡張を 3 行で整理。CLI の入力不便さを「UI レベルで根本的に解消」する選択肢として提示。

### 強み

- 3 種の選択肢を並列で示し、各々の特徴的なショートカット（`Option+K` `Cmd+Option+K` 等）を引用 URL 付きで明示
- 「CLI の入力不便さは Desktop / IDE 統合を使うことで根本的に解消される」（84 行目）の結論が簡潔

### 弱み・指摘

- **IMPORTANT I-07**: Desktop の Shift+Enter 改行は `desktop.md` に記載が無い `[未確認]`。"Press Enter to send" のみ記述、Keyboard shortcuts 表にも改行系ショートカットは無し
- **SUGGESTION S-01**: `/ide` コマンド（外部ターミナル → IDE 連携）への言及が無い

### 引用・参照の検証

| 引用 URL | 検証結果 |
|---|---|
| `vs-code.md` | ✅ Option+K / Alt+K 確認、Shift+Enter（Multi-line input）確認、@-mention 確認 |
| `jetbrains.md` | ✅ Cmd+Option+K / Alt+Ctrl+K 確認、ESC キー設定セクション確認 |
| `desktop.md` | ⚠️ drag-drop ✅ / @mention ✅ / file pane ✅ / Shift+Enter ❌（記載無し） |

---

## 4. 事項(1) D. ファイルベースの永続指示

**ファイル該当範囲**: 88〜101 行目

### 概要評価

CLI プロンプトへの入力量自体を削減する手段として CLAUDE.md / CLAUDE.local.md / path-scoped rules / output-styles を 4 行表で整理。CLAUDE.md の 200 行推奨も併記。

### 強み

- セッション間で繰り返す指示を減らすという観点で B/C と相補的
- 既存ベースキット実装（`./CLAUDE.md`, `.claude/rules/`, `.claude/output-styles/`）との 1:1 対応がとれている

### 弱み・指摘

- **IMPORTANT I-06**: 「CLAUDE.md は 200 行以内が推奨」の一次資料引用が二次経由のみ（調査B 経由）。前回レビュー B5 と同種

### 引用・参照の検証

| 引用先 | 検証結果 |
|---|---|
| 調査報告書 `2026-04-24_調査B.md` | ✅ 28 行目「200 行以内を target、長すぎると `rules/` に分割」を確認 |
| `settings.md` | ✅ `editorMode: "vim"` の項目を確認、CLAUDE.md auto-load の詳細記述は memory.md 側 |
| 一次資料 `memory.md`（要追記） | ✅ 調査E 47 行目「Size: target under 200 lines per CLAUDE.md file...」が記録済み |

---

## 5. 事項(2) 中間成果物の保護方法

**ファイル該当範囲**: 105〜212 行目

### 概要評価

「`.gitignore` は実は安全」という事実切り分けから始まり、5 つの代替アプローチ（現状/① orphan branch / ② 独立リポジトリ / ③ git worktree / ④ クラウド同期）を比較表＋手順例＋判断フローで提示。Git 管理 vs `.gitignore` 除外の判断基準も併記。

### 強み

- **POSITIVE P-02**: `.gitignore` 対象が `git checkout`/`git clean -fd`/`git stash` で保護される事実を先に明示し、過剰対策を防いでいる
- **POSITIVE P-03**: 比較表 → 個別手順 → 判断フロー → 判断基準の三層構造
- **POSITIVE P-04**: 「成果物の昇格パターン」（214〜223 行目）が現実装と整合
- 各代替案にコマンド例があり、すぐに試せる

### 弱み・指摘

- **IMPORTANT I-04**: `gitsubmodules` URL は「nested リポジトリ自動無視」の根拠にならない `[未確認]`
- **IMPORTANT I-08**: ① orphan branch のサンプルコードに `git rm -rf --cached .` の事前実行が抜けている
- **SUGGESTION S-02**: 判断フロー（185〜190 行目）に代替④（クラウド同期）が反映されていない
- **SUGGESTION S-04**: `git checkout` / `git stash` の保護動作の一次引用が欠落

### 引用・参照の検証

| 引用 URL | 検証結果 |
|---|---|
| `git-clean` `-x` 仕様 | ✅ 確認（"Don't use the standard ignore rules ... allowing removing all untracked files, including build products"）|
| `git-checkout --orphan` | ✅ 確認（`git rm -rf .` の必要性も記述あり） |
| `git-worktree` `add` | ✅ 確認 |
| `gitsubmodules` | ⚠️ submodule 機能の解説で「nested 自動無視」の直接根拠ではない |

---

## 6. （参考）Git 管理 vs `.gitignore` 除外の判断基準

**ファイル該当範囲**: 196〜212 行目

### 概要評価

13 行の表で「`.claude/settings.json` ✅ Git 管理」「`.claude/settings.local.json` ❌ 除外」など、ベースキット運用上の管理方針を整理。理由列も併記。

### 強み

- 「チーム共有が必須・再利用可能なもの」 vs 「セッション固有・個人環境差異があるもの」という大原則を最初に置いてから、個別ファイルに展開する構造が明快
- 既存ベースキット実装（`.gitignore` 内容、CLAUDE.md 規約）との整合がとれている

### 弱み・指摘

- 特になし（このセクションは事項(1)(2) の補足として完成度が高い）

### 引用・参照の検証

| 引用先 | 検証結果 |
|---|---|
| 調査B `2026-04-24_調査B_サブフォルダ構造ベストプラクティス.md` | ✅ 存在確認 |
| `cloudnative-co/claude-code-starter-kit/.gitignore` | ⚠️ 実ファイル参照は本レビューで未検証（時間制約のため）。`[未確認]` 該当 |

---

## 引用検証サマリ

WebFetch および直接ファイル読み込みで検証した URL/パスは合計 12 件：

| 種別 | 件数 | 確認結果 |
|---|---|---|
| `code.claude.com/docs/en/` 系 URL | 5 | 全て実在。ただしセクション名の取り違え 2 件（C-03, I-01）|
| `git-scm.com/docs/` 系 URL | 4 | 全て実在。ただし引用根拠が不正確 1 件（I-04: gitsubmodules） |
| 一次資料（external_plugins README.md） | 3 | 全て実在。ただし報告書の引用ファイル名は `PLUGIN.md`（C-01: 不在） |
| 既存調査レポート | 1 件以上 | 調査B・調査E は存在確認 |

`[未確認]` タグ対象（一次資料での直接確認ができなかった項目）：
- I-04: 「Git nested 自動無視」の公式仕様
- I-07: Claude Desktop の Shift+Enter 改行サポート
- 6 章: `cloudnative-co/claude-code-starter-kit/.gitignore` の実ファイル内容
- C-02 関連: Windows 上の Bun ランタイム動作実績
