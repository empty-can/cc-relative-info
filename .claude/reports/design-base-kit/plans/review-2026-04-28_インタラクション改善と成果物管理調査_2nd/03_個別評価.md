# 2 回目クロスレビュー 個別評価詳細

レビュー対象 1 ファイル（`2026-04-28_インタラクション改善と成果物管理調査.md`、Sonnet 4.6 改訂版）の各セクション（A/B/C/D/E + 事項(2)）について、強み・弱み・指摘・引用検証メモを順にまとめる。
指摘の重大度ラベル（CRITICAL / IMPORTANT / SUGGESTION / POSITIVE / `[未確認]`）の意味は `02_指摘事項一覧.md` 凡例参照。

1 回目指摘 16 件はすべて対応反映済み（`01_統合サマリー.md` 対応状況表参照）。本書は **新規追加コンテンツ（cloudnative-co 由来）の裏取り**と**`[未確認]` 再判定**を中心に、各セクションの完成度を再評価する。

---

## 1. 事項(1) A. メッセージングアプリ経由のインタラクション

**ファイル該当範囲**: 23〜43 行目

### 1 回目指摘の対応反映

- **C-01**（PLUGIN.md → README.md）: 完全対応。出典ファイル列が `external_plugins/<name>/README.md` に統一、`ACCESS.md` 補足参照も追加（30〜32 行目）
- **C-02**（Bun 前提条件）: 完全対応。「前提条件」列を新設、Discord/Telegram に `Bun（[未確認]: Windows 動作実績）` 明記（28 行目）
- **I-03**（Telegram download_attachment 誤記）: 完全対応。「Discord は `download_attachment` 経由、Telegram は受信時に自動ダウンロード」と分離（38 行目）
- **I-05**（Discord 欠落手順）: 完全対応。Message Content Intent / Bot サーバー招待 / `/reload-plugins` / `allowlist` 切替を末尾に補記（30 行目）

### 強み

- **POSITIVE 継承**: 1 回目 P-01「メッセージングアプリプラグインの発見」は維持されており、`analyze-claude-code-doc-repos` 経由の調査の幅広さは依然として強み
- 表に「前提条件」列が追加されたことで、Discord/Telegram と iMessage（追加ランタイム不要）の差分が一目で読み取れる
- Discord セットアップに「※ 詳細手順は README.md / ACCESS.md を参照」と注釈し、概要表の簡潔さと運用上の必須ステップ参照の両立を実現

### 弱み・指摘

- 今回新規の指摘なし（1 回目指摘がすべて適切に解消されている）

### 引用・参照の検証

| 引用先 | 検証結果 |
|---|---|
| `external_plugins/discord/README.md` | ✅ 1 回目検証時に Quick Setup 8 ステップ・Tools 表を確認 |
| `external_plugins/telegram/README.md` | ✅ 1 回目検証時に Quick Setup 6 ステップ・Tools 表（download_attachment なし）を確認 |
| `external_plugins/imessage/README.md` | ✅ 1 回目検証時に macOS 専用・Tools 表を確認 |

---

## 2. 事項(1) B. CLI 内での複数行入力改善

**ファイル該当範囲**: 46〜78 行目

### 1 回目指摘の対応反映

- **C-03**（interactive-mode "Chat actions" 不在）: 完全対応。56 行目を `keybindings.md: "Chat actions"` に修正、`#chat-actions` アンカー付与
- **I-01**（terminal-config 節タイトル誤り）: 完全対応。53〜54 行目を `"Enter multiline prompts"` に統一、`#enter-multiline-prompts` アンカー付与
- **I-02**（Shift+Enter 区分巻き込み）: 完全対応。61〜67 行目で 3 区分（ネイティブ対応 / `/terminal-setup` / 非対応）を再構成
- **S-03**（節アンカー）: 対応。引用 URL 全般に `#section-id` 付与

### 強み

- 表全体の操作・出典の対応関係が明確化。`Ctrl+J` / `Ctrl+G` / `Ctrl+S` / `@filename` / 自動圧縮ペースト / 画像ペーストを一覧化
- Shift+Enter 対応状況を 3 区分の独立表として再構成したことで、「Windows Terminal は非対応」「Ghostty/Kitty/iTerm2 は設定不要」が誤解なく伝わる
- 71〜72 行目「`Ctrl+G`（外部エディタ統合）が最実用的」のように、表に出てこない実用判断を本文で補強

### 弱み・指摘

- **CRITICAL C2-01**: 59 行目「画像・スクリーンショットを渡す」行
  - 「`[未確認]`: 公式ドキュメントへの明示的記載なし」は誤り。公式 `keybindings.md#chat-actions` に `chat:imagePaste | Ctrl+V (Alt+V on Windows) | Paste image` として **明示記載されている**
  - **Windows のデフォルトキーは `Alt+V`**（`Ctrl+V` ではない）。報告書の `Win+Shift+S → Ctrl+V` は事実誤りの可能性
  - 詳細: `02_指摘事項一覧.md` C2-01 参照
- **SUGGESTION S2-01**: C2-01 の対応として、出典列を `[keybindings.md: "Chat actions"](https://code.claude.com/docs/en/keybindings#chat-actions)` に変更し、`[未確認]` タグ削除・Windows キー訂正を同時に行うことを推奨

### 引用・参照の検証

| 引用 URL | 実在 | セクション存在・記載確認 |
|---|---|---|
| `terminal-config.md#enter-multiline-prompts` | ✅ | 1 回目検証で確認済み（"Enter multiline prompts" 節）|
| `terminal-config.md#paste-large-content` | ✅ | 1 回目検証で確認済み |
| `terminal-config.md#edit-prompts-with-vim-keybindings` | ✅ | 1 回目検証で確認済み |
| `interactive-mode.md#general-controls` | ✅ | 1 回目検証で確認済み |
| `keybindings.md#chat-actions` | ✅ | 今回 WebFetch で `chat:submit` `chat:newline` `chat:stash` `chat:externalEditor` `chat:imagePaste` 等を直接確認 |
| `keybindings.md`（カスタマイズ用引用） | ✅ | 同上。`bindings` 配列・`Chat` context への割り当て例も確認 |
| `common-workflows.md#reference-files-and-directories` | ✅ | 1 回目検証で確認済み |
| `cloudnative-co/.../README.md`（59 行目参考） | ⚠️ | 引用は事実だが、**公式 keybindings.md を引用すれば `[未確認]` タグ不要**（C2-01）|

---

## 3. 事項(1) C. IDE 統合 / Desktop アプリ

**ファイル該当範囲**: 82〜94 行目

### 1 回目指摘の対応反映

- **I-07**（Desktop Shift+Enter 未確認）: 完全対応。88 行目に `[未確認]` タグ付与
- **S-01**（/ide コマンド欠落）: 対応。94 行目に「外部ターミナルから IDE の diff viewer 等の連携機能を使うには `/ide` コマンドを実行する」を追記

### 強み

- 3 種の選択肢（Desktop / VS Code / JetBrains）を並列で示し、各々のショートカット（`Option+K` `Cmd+Option+K`）を引用 URL 付きで明示する構造は維持
- `/ide` コマンドへの追記で、外部ターミナル運用 → IDE 連携の経路もカバー

### 弱み・指摘

- **`[未確認]` 維持判定**: 88 行目「Shift+Enter で改行 `[未確認]`」
  - 今回 WebFetch で `desktop.md` を再取得し、`shift+enter` / `multiline` / `multi-line` / `newline` / `line break` / `press enter` を grep → **すべて 0 件ヒット**
  - Desktop 公式ドキュメント側で改行系ショートカットは依然として未記載のため、`[未確認]` タグ維持が妥当

### 引用・参照の検証

| 引用 URL | 検証結果 |
|---|---|
| `vs-code.md` | ✅ 1 回目検証時に Option+K / Alt+K・Shift+Enter（Multi-line input）・@-mention 確認 |
| `jetbrains.md` | ✅ 1 回目検証時に Cmd+Option+K / Alt+Ctrl+K・ESC キー設定セクション確認 |
| `desktop.md` | ⚠️ Shift+Enter 系の記述は **依然として未記載**（今回 WebFetch 全文 grep で確認）。`[未確認]` タグ維持で妥当 |

---

## 4. 事項(1) D. ファイルベースの永続指示

**ファイル該当範囲**: 98〜111 行目

### 1 回目指摘の対応反映

- **I-06**（200 行公式リンク）: 完全対応。111 行目に `[memory.md](https://code.claude.com/docs/en/memory)` 直接リンク追加（調査B 二次引用との併記）

### 強み

- セッション間で繰り返す指示を減らすという観点で B/C と相補的（`Ctrl+G` での編集と CLAUDE.md による永続化の組み合わせは現実装と整合）
- 既存ベースキット実装（`./CLAUDE.md`, `.claude/rules/`, `.claude/output-styles/`）との 1:1 対応がとれている

### 弱み・指摘

- 今回新規の指摘なし

### 引用・参照の検証

| 引用先 | 検証結果 |
|---|---|
| 調査報告書 `2026-04-24_調査B.md` | ✅ 1 回目検証で確認済み |
| `memory.md`（公式） | ✅ 1 回目検証で「Size: target under 200 lines per CLAUDE.md file...」を確認 |
| `settings.md` | ✅ 1 回目検証で `editorMode: "vim"` 等を確認 |

### `[未確認]` 関連メモ

- `00_レビュー前インプット.md` で言及されていた「cloudnative-co は 150 行以内、公式は 200 行以内」の差分は、本セクションでは公式基準（200 行）を採用しており、ポリシーに整合

---

## 5. 事項(1) E. セッション管理（新規追加セクション）

**ファイル該当範囲**: 113〜148 行目

cloudnative-co 由来コンテンツとして 1 回目以降に新規追加されたセクション。本セクションの精査が今回レビューの主要対象の一つ。

### 概要評価

- セッション命名（`claude -n` / `/rename` / `claude -r`）、`/handover`、コンパクションのタイミング最適化を 3 ブロック構造で提示
- 公式機能（CLI フラグ）と非公式機能（cloudnative-co のスラッシュコマンド・スキル）が混在しているが、参考実装として明示

### 強み

- **公式 vs 非公式の出典区別が明示されている**:
  - `claude -n` / `claude -r` は `cli-reference.md` を直接引用（公式）
  - `/handover` には「（参考: `cloudnative-co/claude-code-starter-kit`）」と前置き
  - 138〜148 行目「コンパクションのタイミング最適化」は cloudnative-co `strategic-compact` スキルを出典として明示
- セッション命名と再開の組み合わせ（`claude -n` 起動時 → `/rename` 途中変更 → `claude -r` 再開）が一筆で示されており、再現可能な手順として読める
- 命名パターン例（125 行目）が具体的で、読者が即座に応用可能

### 弱み・指摘

- **IMPORTANT I2-01**: 122 行目「セッション途中で名前を変更 / `/rename [feature-name: current-status]` / 出典: `cli-reference.md#cli-flags`」
  - `/rename` はスラッシュコマンドであり CLI フラグではない
  - `cli-reference.md` 内では `--name/-n` フラグの説明欄で `/rename` が言及されているのみ
  - アンカーは `#cli-commands` または別の一次資料へ修正すべき
- **SUGGESTION S2-02**: 125 行目「命名パターン例: `[auth-refactor: plan-approved]` / `[bug-123: investigating]` / `[sprint-12: day-3]`」
  - これらは公式の推奨命名規則ではなく、cloudnative-co の `/handover` コマンド実装で示されているパターン（Memory MCP cloudnative-co:command/handover で確認）
  - 「（参考: cloudnative-co/`commands/handover.md` の推奨パターン）」と注記するか、cloudnative-co 由来であることを E セクション冒頭で改めて明示するのが望ましい

### 引用・参照の検証

| 引用 URL / 一次資料 | 検証結果 |
|---|---|
| `cli-reference.md` `--name/-n` | ✅ 今回 WebFetch で「`--name`, `-n` ... You can resume a named session with `claude --resume <name>`」を直接確認 |
| `cli-reference.md` `/rename`（122 行目） | ⚠️ `cli-reference.md` 内で `/rename` はフラグ説明欄に言及あるのみ。出典アンカー `#cli-flags` は不正確（I2-01）|
| `cli-reference.md` `--resume/-r` | ✅ 今回 WebFetch で「`--resume`, `-r`: Resume a specific session by ID or name」を直接確認 |
| cloudnative-co `/handover` | ✅ Memory MCP cloudnative-co:command/handover エンティティでパターン・出力構造を確認 |
| cloudnative-co `strategic-compact` | ✅ Memory MCP cloudnative-co:skill/strategic-compact エンティティで「Optimal timing」3 種を確認 |

---

## 6. 事項(2) 中間成果物の保護方法

**ファイル該当範囲**: 152〜302 行目

### 1 回目指摘の対応反映

- **I-04**（gitsubmodules 引用根拠）: 完全対応。212〜213 行目で出典を `gitignore` に変更、`[未確認]` タグ付与
- **I-08**（orphan branch クリーンアップ抜け）: 完全対応。184 行目に `git rm -rf --cached .` 追加
- **S-02**（判断フロー ④ 未反映）: 対応。295 行目で「クラウド同期フォルダ内?」を最初の分岐に追加
- **S-04**（git-checkout/stash 引用）: 対応。161〜163 行目で git-checkout / git-stash の一次引用を追加

### 概要評価

- 1 回目の三層構造（比較表 → 個別手順 → 判断フロー → Git 管理 vs `.gitignore` 除外の判断基準）に、新たに **⑤ PreCompact フック + 自動コミット** が追加された
- `/checkpoint` 補足（258〜269 行目）と Safety Net 補足（271〜290 行目）も新設
- 比較表の「保護強度」評価は妥当（⑤ は「中（フック発火タイミング依存）」と適切に位置付け）

### 強み

- **POSITIVE P2-04**: ⑤ PreCompact フックを「①〜④と**併用**」「Git 管理下のファイルのみ対象」「`.gitignore` 除外ファイルの保護には ①〜④ を別途組み合わせること」と明示。⑤ 単独で成果物保護を完結させない補完的役割であることを誤解の余地なく示している（177 行目・253〜254 行目）
- 比較表に「向いているケース」列があり、各代替の選択基準が一目で分かる
- 判断フロー（294〜302 行目）に ④ クラウド同期が最初の分岐として組み込まれ、過剰な代替実装に走らない判断ルートが明示されている

### 弱み・指摘

- **CRITICAL C2-02**: Safety Net 補足 280 行目 — `"matcher": "tool == \"Bash\""` は公式 `hooks.md` 仕様外の式形式
  - 公式に動作する matcher は単純なツール名（`"Bash"`）、パイプ区切り（`"Edit|Write"`）、正規表現、MCP パターンのみ
  - cc-safety-net パッケージ内部での独自記法か、cloudnative-co の `features/safety-net/hooks.json` を逐語コピーした結果（一次資料を直接確認）
  - 詳細: `02_指摘事項一覧.md` C2-02 参照
- **SUGGESTION S2-03**: ⑤ 設定例 255 行目「フック仕様: [hooks.md: PreCompact](https://code.claude.com/docs/en/hooks)」のアンカーが `#hooks`（先頭）のままで、節アンカーが付いていない。S-03 で導入したアンカー方針との整合性

### `[未確認]` 関連メモ

- 212〜213 行目「`[未確認]`: 「自動無視」を直接裏付ける公式仕様」は WebFetch で `gitignore` ページを再確認 → embedded git repository の自動無視に関する明示記述は依然としてなし。**`[未確認]` 維持で妥当**
- ⑤ matcher: "" の妥当性 → 今回 WebFetch で「`""`, `"*"`, または省略 = Match all」と公式確認済み。**問題なし**
- ⑤ command 内 `cd "$CLAUDE_PROJECT_DIR"` → 今回 WebFetch で「`$CLAUDE_PROJECT_DIR` 環境変数自体は使用できます」と確認済み（PreCompact での使用に問題なし）。**問題なし**

### 引用・参照の検証

| 引用 URL / 一次資料 | 検証結果 |
|---|---|
| `git-clean` `-x` 仕様 | ✅ 1 回目検証で確認済み |
| `git-checkout --orphan` | ✅ 1 回目検証で確認済み |
| `git-checkout` `Local changes` | ✅ 1 回目検証で確認済み（S-04 対応） |
| `git-stash` | ✅ 1 回目検証で確認済み（S-04 対応） |
| `git-worktree` `add` | ✅ 1 回目検証で確認済み |
| `gitignore`（②・212〜213 行目） | ✅ ページ実在は確認、ただし「nested 自動無視」を直接裏付ける記述はなし → `[未確認]` 維持 |
| `hooks.md: PreCompact`（⑤・255 行目） | ✅ 公式存在確認、matcher: ""・`$CLAUDE_PROJECT_DIR` 使用も整合性確認 |
| `hooks.md: PreToolUse`（Safety Net・290 行目） | ⚠️ ページは公式だが、配下で示された matcher 式形式（`tool == "Bash"`）は **公式仕様外**（C2-02）|
| `cloudnative-co/.../features/pre-compact-commit/hooks.json` | ✅ 一次資料直接読み込みで matcher・command を逐語確認 |
| `cloudnative-co/.../features/safety-net/hooks.json` | ✅ 一次資料直接読み込みで matcher: `tool == "Bash"`・command: `cc-safety-net --claude-code` を確認（C2-02 の根拠）|
| `cloudnative-co/.../commands/checkpoint.md` | ✅ Memory MCP cloudnative-co:command/checkpoint エンティティで内容確認 |

---

## 7. （参考）Git 管理 vs `.gitignore` 除外の判断基準

**ファイル該当範囲**: 306〜323 行目

### 概要評価

- 1 回目から構造変更なし。13 行の表で各ファイルの管理方針と理由を整理

### 強み

- 「チーム共有が必須・再利用可能」 vs 「セッション固有・個人環境差異」の大原則を最初に置く構造は維持
- 既存ベースキット実装（`.gitignore` 内容、CLAUDE.md 規約）との整合がとれている

### 弱み・指摘

- 今回新規の指摘なし（1 回目の「特になし」を維持）

---

## 8. 成果物の「昇格」パターン

**ファイル該当範囲**: 325〜335 行目

### 強み

- **POSITIVE 継承**: 1 回目 P-04「成果物の昇格パターンが現実装と整合」は維持
- `.claude/reports/<activity>/` → `CLAUDE.md` / `rules/` / `skills/` への 3 経路が、近時の skills 昇格コミット（`8bb15ae` `af3ef55`）と整合

### 弱み・指摘

- 今回新規の指摘なし

---

## 9. 未確認項目セクション

**ファイル該当範囲**: 339〜344 行目

### 概要評価

- 1 回目 S-05「未確認項目セクション欠落」への対応として新設
- 4 項目を箇条書きで列挙

### 強み

- **POSITIVE P2-02**: インライン `[未確認]` タグと末尾セクションの二重構造で、確認済み事実と未確認項目の区別が容易

### 弱み・指摘

- **IMPORTANT I2-02**: 344 行目「Ctrl+V によるスクリーンショット/画像ペーストのサポート（公式 Claude Code ドキュメントへの明示的記載は未確認）」は事実誤認。公式 `keybindings.md#chat-actions` に `chat:imagePaste` として明示記載されているため、項目自体を削除（または事実訂正）すべき。C2-01 と一括対応

### 各項目の再判定

| 項目 | 報告書記載箇所 | 検証 | 判定 |
|---|---|---|---|
| 341 行目: Windows 上の Bun 動作実績 | ✅ 維持 | 文書調査では解決不可 | 維持 |
| 342 行目: Desktop の Shift+Enter 改行 | ✅ 維持 | `desktop.md` 全文 grep → 0 件 | 維持 |
| 343 行目: nested リポジトリ自動無視公式仕様 | ✅ 維持 | `gitignore` ページに記載なし | 維持 |
| 344 行目: Ctrl+V スクリーンショット | ❌ 削除/訂正 | `keybindings.md` に **明示記載あり** | **解消必須** |

---

## 引用検証サマリ

WebFetch および直接ファイル読み込みで検証した URL/パスは合計 11 件（うち今回新規検証は 5 件）：

| 種別 | 件数 | 確認結果 |
|---|---|---|
| `code.claude.com/docs/en/` 系 URL（今回新規 WebFetch） | 5 | `desktop.md` / `keybindings.md` / `cli-reference.md` / `hooks.md` の全実在を再確認。`keybindings.md` の `chat:imagePaste` 公式記載が新規発見（C2-01 根拠） |
| `git-scm.com/docs/` 系 URL | 1（今回新規） | `gitignore` 再取得し、nested リポジトリ自動無視の記載なしを確認（`[未確認]` 維持判断の根拠） |
| 一次資料（cloudnative-co hooks.json） | 2（今回新規） | `features/pre-compact-commit/hooks.json` で matcher/command を逐語確認、`features/safety-net/hooks.json` で matcher 式形式を確認（C2-02 根拠） |
| Memory MCP（cloudnative-co エンティティ） | 20 エンティティ参照 | StarterKit / StarterKitMemoryDoc / StarterKitCommand / StarterKitSkill / StarterKitFeature / StarterKitRule すべて参照、E セクションと事項(2) 補足の出典確認に活用 |
| 既存調査レポート（調査B 等） | 1 件以上 | 1 回目検証で存在確認済み |

`[未確認]` タグ対象（一次資料での直接確認ができなかった項目）の最終判定：
- I-04（nested 自動無視）: **維持**（gitignore に記載なし確認）
- I-07（Desktop Shift+Enter）: **維持**（desktop.md に記載なし確認）
- C-02 関連（Windows Bun）: **維持**（文書調査では解決不可）
- 旧「Ctrl+V スクリーンショット」: **解消必須**（keybindings.md に明示記載あり）
- 新規候補: cc-safety-net パッケージの `tool == "Bash"` 形式 matcher の実機動作（C2-02 関連、cc-safety-net 内部実装に依存）
