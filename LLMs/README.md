# LLMs

LLM に関する情報収集を行うフォルダ。

## 目的

1. Claude Code の利用に有用な各種公式団体の公式ドキュメントの `llms.txt` を定期的に取り込む
2. `llms.txt` の存在しないドキュメント・リポジトリの `llms.txt` を作成する仕組みを構築する

## 現状

フォルダのみ作成済み。取り込み対象一覧・取り込みスクリプト・生成仕組みは未整備。
