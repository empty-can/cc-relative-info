# クロスレビュー報告書 — 作業指示者＋人間読み手観点（v2.0 対象）

## はじめに

### 本レビューについて

| 項目 | 内容 |
|---|---|
| レビュー対象 | `../結論・構成案_ポータブルな.claude共有_v2.0.md`（976 行） |
| レビューイ | Claude Opus 5（レビュー対象ドキュメント作成者） |
| レビュアー | Claude Opus 5（**作業指示者の代理**。人間の読み手が実際に困る点を先回りして洗い出す役割） |
| レビュー区分 | 作業指示者＋人間読み手観点 |
| レビュー日 | 2026-09-14 |
| 版 | 0.9 |
| 想定読了時間 | 35 分 |

> **本報告書の位置づけ**: 本観点は本来、作業指示者（人間）が担当する。本報告書は**作業指示者レビュー（1.0 版）の前段**として、代理レビューアが下流成果物との突き合わせを含めて先行実施したもの。1.0 版では作業指示者が各指摘に態度表明を行う。

#### レビュー観点

* 読み手の意思決定を支えるか
* 読みやすさ・たどりやすさ
* **下流成果物との整合**（本レビューの重点）
* 運用に移したときの現実性

#### 判断基準

* レビュー対象 v2.0 本文
* `../結論・構成案_ポータブルな.claude共有_v1.2.md`（時点記録）／`../drafts/v2.0_章立て骨子.md`
* 下流成果物: `reports/02.配布物の開発・テスト/` 配下／`reports/03.実装テンプレート（層1+2）/README.md`／`reports/04.資産インベントリ・統合/` 配下／`claude-dir-sharing-governance/CLAUDE.md`

#### スコープ外

* 案C-1 を採用するという方針そのものの是非（作業指示者決定事項）
* `rebase` による追従方針の是非（同上）
* 「必要な情報を本書内に保持する」執筆方針の是非（同上）
* v1.2 を時点記録として残すこと自体の是非（同上）
  * ※ U-C2 は「v1.2 を残すこと」ではなく「**正本が二重化し下流が旧側を指したままであること**」への指摘であり、スコープ内

### 版数とレビュー指摘件数（重要度別）

#### CRITICAL 件数推移

| 観点 | 0.8 | 0.9 | 1.0 |
|---|---|---|---|
| 読み手の意思決定を支えるか | 0 | 同左 | <TBD> |
| 読みやすさ・たどりやすさ | 0 | 同左 | <TBD> |
| 下流成果物との整合 | 4 | 同左 | <TBD> |
| 運用に移したときの現実性 | 0 | 同左 | <TBD> |
| **合計** | **4** | **同左** | <TBD> |
| 取り下げ累計 | — | 0 | <TBD> |

#### IMPORTANT 件数推移

| 観点 | 0.8 | 0.9 | 1.0 |
|---|---|---|---|
| 読み手の意思決定を支えるか | 2 | 同左 | <TBD> |
| 読みやすさ・たどりやすさ | 1 | 同左 | <TBD> |
| 下流成果物との整合 | 1 | 同左 | <TBD> |
| 運用に移したときの現実性 | 3 | 同左 | <TBD> |
| **合計** | **7** | **同左** | <TBD> |
| 取り下げ累計 | — | 0 | <TBD> |

#### SUGGESTION 件数推移

| 観点 | 0.8 | 0.9 | 1.0 |
|---|---|---|---|
| 読み手の意思決定を支えるか | 2 | 同左 | <TBD> |
| 読みやすさ・たどりやすさ | 2 | 同左 | <TBD> |
| 運用に移したときの現実性 | 1 | 同左 | <TBD> |
| **合計** | **5** | **同左** | <TBD> |
| 取り下げ累計 | — | 0 | <TBD> |

#### POSITIVE 件数推移

| 観点 | 0.8 | 0.9 | 1.0 |
|---|---|---|---|
| 読み手の意思決定を支えるか | 1 | 同左 | <TBD> |
| 読みやすさ・たどりやすさ | 1 | 同左 | <TBD> |
| 運用に移したときの現実性 | 3 | 同左 | <TBD> |
| **合計** | **5** | **同左** | <TBD> |

### 判断依頼サマリー

#### CRITICAL（4 件、判断依頼対象 3 件）

| ID | タイトル | 差し戻しリスク | 態度表明 |
|---|---|---|---|
| [U-C3](#u-c3-既にリリース済みの配布キットが-6-2-案c-2不採用そのものだが移行手順にその扱いが無い) | 既存配布キット（C-BDK/C-BDC/C-BCP・v1.0.1 リリース済み）の去就が書かれていない | **高**。案C-1 の共有リポは構造的に C-BDC そのもの。流用か新規作成かが決まらないと移行に着手できない。**3 リポの去就は作業指示者の判断事項** | （未記入） |
| [U-C4](#u-c4-5-6-のランチャーが既存ランチャーc-bdk-docslauncher正本との関係を示さない) | §5-6 のランチャーと既存ランチャー（同名・6 ファイル構成・`custom.env` で秘匿管理）の関係が不明 | **高**。同名ファイルの衝突に加え、**秘匿値の受け皿が消えており、共有リポの `settings.json` へ流れると全メンバーに配布される退行リスク** | （未記入） |
| [U-C1](#u-c1-reports03実装テンプレート層12-が-v12-推奨の実装のまま取り残され本書に一切の言及が無い) | 03 実装テンプレートが v1.2 §推奨の実装のまま取り残されている | 中〜高。雛型をコピーしたチームが、v2.0 が最大の問題と断じた「複製」を新規に作ってしまう | （未記入） |

> U-C2（正本の二重化）はレビューイ完全同意＋レビューア受容の追認判断のため除外基準 2 により除外。**ただし下流 5 文書の編集を伴うため、実施の可否は作業指示者の確認を得たい**。

#### IMPORTANT（7 件、判断依頼対象 3 件）

| ID | タイトル | 差し戻しリスク | 態度表明 |
|---|---|---|---|
| [U-I1](#u-i1-案c-1-での開発テスト手順が空白で-02-編の既存手順方法b--add-direnvsettingsは成立しなくなる) | 案C-1 でのブランチ検証手順が空白（02 編の方法B が成立しなくなる） | 中。未検証ブランチの push で全メンバーが壊れた `.claude/` を掴む経路が開く | （未記入） |
| [U-I2](#u-i2-5-6-の非対称reset---hardが本リポジトリを主語に説明されチームが設定すべき-deny-ルールの指示が無い) | `reset --hard` の非対称が「本リポジトリ」を主語に説明され、読者の設定に翻訳できない | 中。deny を設定しないまま周知だけ行われる。**ランチャー経由の抜け道も未言及** | （未記入） |
| [U-I3](#u-i3-8-2-の未検証項目に誰がいつ確認するかと案c-1-の採否をブロックするかが無い) | 未検証項目に「採否をブロックするか」「担当」が無い | 中。慎重な読者は V1 待ちで採用判断を止め、積極的な読者は V6 未検証のまま展開する | （未記入） |

---

## CRITICAL

### U-C1. `reports/03.実装テンプレート（層1+2）/` が v1.2 §推奨の実装のまま取り残され、本書に一切の言及が無い

**観点**: 下流成果物との整合

#### U-C1. 問題点

* **概要**: 03 の雛型は「層1＝各リポジトリへコピー／commit」という v1.2 の配布機構を実装したもので、v2.0 が「複製をやめて単一 clone にする」と決めた方針と正面から衝突する。にもかかわらず v2.0 は 03 に一度も触れていない。
* **問題点**:
  1. 03 README の前提が v1.2 §推奨であり、v2.0 の案C-1 では成立しない配布機構（各リポへのコピー展開）を指示し続けている
  2. v2.0 本文・変更履歴・§7 移行手順のいずれにも「03 の雛型をどう扱うか（廃止／改修／読み替え）」の記載が無い
  3. テーマ索引（`claude-dir-sharing-governance/CLAUDE.md` 成果物表）でも 03 に注記が付いていない
* **影響**:
  1. 03 をコピーしたチームは、**v2.0 が最大の問題と断じた「共通資産の実体が各リポに複製され、どれが最新か分からなくなる」状態を新規に作ってしまう**
  2. 読者が「v2.0 を読んだ後、次に何を触ればよいか」を判断できない
  3. 索引経由で 03 に直行した読者は v2.0 の存在自体に気づかない
* **詳細**:
  1. 03 README 冒頭は「方針レポート（`…_v1.2.md`）の **§推奨** を、コピーして使える**ディレクトリ骨格**へ落とした実装雛形」と宣言。`layer1-repo-template/` の位置づけも「**利用先リポジトリの直下へコピー／commit する最小骨格**」
  2. さらに `.claude/CLAUDE.md` を「共通ガバナンス（README 隔離・**`--add-dir`+env でロード**）」と説明しており、案C-1（project スコープで無条件ロード・フラグ不要）とロード経路の説明が食い違う
  3. **内容そのものは案C-1 の「共有 `.claude` リポジトリ `main` ブランチの初期中身」としてほぼそのまま流用できる**。必要なのは全面作り直しではなく**位置づけの読み替え宣言**である
* **根拠**:
  1. `reports/03.実装テンプレート（層1+2）/README.md` の冒頭・`layer1-repo-template/` の説明・`.claude/CLAUDE.md` 行
  2. v2.0 §6-1 比較表の「現状維持（submodule / 各リポに複製）」列／§6-2「共通資産の実体が複数になり…どのリポジトリが最新の規約で動いているかが誰にも分からなくなる」
  3. v2.0 全文を `実装テンプレート|配布物の開発|資産インベントリ` で検索して**ヒット 0 件**

##### U-C1. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **指摘内容に対する同意の程度の根拠**:
  * v2.0 執筆時、下流成果物への影響を「本書のスコープ外」と暗黙に扱っていた。しかし本書は**テーマの正本**であり、正本が下流の位置づけを示さないのは責任範囲の放棄にあたる
* **判断材料の追加情報**:
  * 論理整合性観点 S5 も同じ論点を独立に提起している（ただし推測ベースのため SUGGESTION）。**2 名のレビューアが一致して指摘している**
  * レビューアの指摘 3)（内容は流用できる）は重要で、**03 の価値を捨てずに済む**。`layer1-repo-template/.claude/` の中身は案C-1 の共有リポ `main` の初期コミットとしてそのまま使える

##### U-C1. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが正本としての責任範囲を認め、別観点との一致も確認した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-C1. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §7 の直後に「§7-5 既存成果物の扱い」を新設し、03／02／04 それぞれについて「現行方針での位置づけ（有効／読み替え／凍結）」を 1 行ずつ表で宣言する | **読者が次に触るべき資産を 1 箇所で判断できる**。本書を正本のまま保てる | 下流が動くたび追記が要る |
| 2 | 03 README の冒頭にバナーを付ける | 03 に直行した読者を確実に捕まえられる | v2.0 側からは 03 の存在が見えないまま |
| 3 | 1 と 2 の両方 | 双方向に導線が張れる | 記述が 2 箇所になる（内容は「位置づけ」だけなのでドリフトは小さい） |

1. **修正案1詳細**: 表の列は「成果物 ／ 現行方針での位置づけ ／ 読み替え規則 ／ 対応要否」。03 は「読み替え」＝`layer1-repo-template/.claude/` の中身をそのまま共有 `.claude` リポジトリ `main` の初期コミットに使う、`layer2-plugin/` は §5-2「リポジトリ固有の機能資産は plugin」の実装としてそのまま有効、と書く

##### U-C1. レビューイからの見解・指摘・補足（修正案）

* **修正案3（1 と 2 の両方）を採用する。** 修正案1 だけでは 03 に直行した読者が救われず、修正案2 だけでは本書の正本としての責任が果たせないため。
* **採用推奨案の拡張提案**: §7-5 の表に「**最終確認日**」列を足す。下流が動くたび追記が要るというデメリットに対し、「いつ時点の評価か」が分かれば陳腐化しても誤誘導にならない。

##### U-C1. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: レビューイの両採用はデメリット（記述が 2 箇所）を受け入れたうえでの判断で妥当。最終確認日の列は、修正案1 の唯一のデメリットへの直接の緩和策
* **最終結論・最終修正案**: 修正案3 ＋ §7-5 の表に「最終確認日」列を追加
* **収束ステータス**: 達成

#### U-C1. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-C2. `--add-dir` 例外ロード一覧の「正本」が v1.2 と v2.0 に二重化し、下流 5 文書は v1.2 を指したまま

**観点**: 下流成果物との整合

#### U-C2. 問題点

* **概要**: v2.0 付録B B-2 は自らを「正本」と宣言し「食い違ったら本表を優先」と書いているが、v1.2 付録B も同じ宣言を保持しており、下流 5 文書は v1.2 側だけを参照している。
* **問題点**:
  1. 同一の表について「正本」を名乗る文書が 2 つ存在し、どちらが勝つかを決める記述がどこにも無い
  2. 下流 5 文書は v1.2 のアンカー `#adddir-exceptions` を指しており、v2.0 が正本になったことを知る手段が無い
  3. 2 つの表は既に内容が分岐している（版境界の置き場所・Agent SDK 行の扱い）
  4. **v1.2 は「時点記録」と宣言されているのに、実際には v2.0 と同日（2026-09-14）に E2 として制御機構表が追記されており、凍結されていない**
* **影響**:
  1. 次に版依存の事実が変わったとき、片方だけ更新される。しかも「どちらを更新すべきか」自体が決まっていない
  2. 手順書に従う実務者は v1.2 に着地し、v2.0 の存在も案C-1 も知らずに `--add-dir` 前提の作業を続ける
  3. 既に分岐しているため「両方読めば同じ」という救済も効かない
  4. **「v1.2 は凍結された時点記録だから参照先として安定している」（§6 冒頭）という本書の主張自体が事実と合わない**
* **詳細**:
  1. v1.2 側は従属先として 02 編 3 文書を**名指し**しており、v2.0 側は誰も名指ししていないため、現場の参照は v1.2 に向き続ける
  2. 分岐の実体: v2.0 の agents 行は「live reload なし」のみで**版境界（`v2.1.178+`）が本文から消え**注記へ移った。commands 行も同様。v1.2 は両方を表本文に持つ。下流 5 文書は「**版依存の事実**は正本を正とする」と書いているので、参照先を切り替えると探す場所が変わる
  3. レーンA（テーマ `CLAUDE.md` が「リリース工程の正本」と位置づけ、凍結バナーが無い**生きた**文書）も v1.2 付録B を正本参照している。しかも参照先の「層3 でランチャーが起動不能になる警告表」は、v2.0 では付録B ではなく **§4-3** に移っている
* **根拠**:
  1. v2.0 付録B B-2 の正本宣言／v1.2 付録B の正本宣言（従属先 3 文書を名指し）
  2. v1.2 を参照している下流 5 文書: `02.Marketplace外資産編/Marketplace外資産_開発・テスト_手順書.md` L145 ／ `02.Marketplace外資産編/Marketplace外資産の開発・テスト_調査結果.md` L68 ／ `01.Plugin・Marketplace編/Plugin・Marketplace配布物の開発・テスト_調査結果.md` L159 ／ `01.Plugin・Marketplace編/Plugin開発・テスト_手順書.md` L496 ／ `04.資産インベントリ・統合/04.ランチャースクリプト実装/配布・リリース設計確定_レーンA.md` L64
  3. v1.2 が凍結されていない証跡: v1.2 の「⚠ 例外ロードを絞る制御機構（**2026-09-14 追加・E2**）」

##### U-C2. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **指摘内容に対する同意の程度の根拠**:
  * 特に 4)（v1.2 が凍結されていない）は**レビューイの記述そのものの誤り**である。v2.0 §6 冒頭で「凍結された時点記録なので参照先として安定している」と書いたが、**同じ日に v1.2 へ E2 を追記したのはレビューイ自身**であり、事実と異なる主張を書いた
* **判断材料の追加情報**:
  * 論理整合性観点 P4 でも同じ矛盾が指摘されている（POSITIVE の追加コメントとして）
  * 下流 5 文書の該当行を確認した。いずれも 1 行のリンク張り替えで済み、**内容の書き換えは不要**

##### U-C2. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが自らの記述の誤りとして認定し、修正範囲（1 行 × 5 文書）も確認した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-C2. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | v2.0 B-2 を単一の正本と宣言し、①v1.2 付録B の当該節冒頭に「本表は時点記録。現行の正本は v2.0 付録B B-2」バナーを置く ②下流 5 文書の参照先を v2.0 へ張り替える ③v2.0 B-2 に従属文書 5 件を名指しで列挙する | **正本が 1 つに戻り、正本一元化の設計意図が回復する**。次回更新時の同期先が機械的に分かる | 下流 5 文書の編集が発生する（各 1 行） |
| 2 | v2.0 B-2 から「正本」の語を外し「v1.2 付録B（正本）の再掲」と明記する | 編集量が最小 | **「設計判断の根拠には v2.0 を使え」と矛盾する。時点記録に正本を置き続ける不自然さが残る** |
| 3 | v2.0 B-2 の版境界を表本文へ戻したうえで 1 を実施 | 下流の「版依存の事実は正本を正とする」という記述と粒度が揃う | B-2 の表が横に伸びる |

1. **修正案1詳細**: ③の名指しは必須。v1.2 が従属先を名指ししていたからこそ正本一元化が機能した。v2.0 側に名簿が無いと同じ失敗を繰り返す。あわせてレーンA の参照先は「v2.0 §4-3」へ張り替える

##### U-C2. レビューイからの見解・指摘・補足（修正案）

* **修正案1 ＋ 修正案3 を採用する。** 修正案3（版境界を表本文へ戻す）は、下流 5 文書が「**版依存の事実**は正本を正とする」と書いている以上、正本側が版境界を表から外しているのは粒度の不整合にあたるため。B-2 の表は 6 行しかなく、版境界を 1 列足しても許容範囲と判断する。
* **採用推奨案の拡張提案**: v1.2 に置くバナーは**付録B の節だけでなく文書冒頭にも**置く。v1.2 は付録B 以外（付録A・§マトリクス）も参照されており、節単位のバナーでは他節から入った読者に届かないため。

##### U-C2. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 修正案3 の併用は下流の記述粒度と揃えるもので合理的。文書冒頭へのバナー追加は、v1.2 が複数箇所から参照されている実態（`#appendix-a`・`#matrix` も参照あり）に照らして必要
* **最終結論・最終修正案**: 修正案1 ＋ 修正案3 ＋ v1.2 の文書冒頭へのバナー追加
* **収束ステータス**: 達成

#### U-C2. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-C3. 既にリリース済みの配布キットが §6-2「案C-2＝不採用」そのものだが、移行手順にその扱いが無い

**観点**: 下流成果物との整合 ／ 運用に移したときの現実性

#### U-C3. 問題点

* **概要**: 本テーマは `.claude` 本体を独立リポ（C-BDC）にし、雛型リポ（C-BCP）が **git submodule** として抱える構成を v1.0.1 まで実リリース済みである。v2.0 はこの形（案C-2）を明確に不採用としながら、既存キットの存在・publish-share リリース工程・3 リポの今後に一言も触れていない。
* **問題点**:
  1. §7 の出発点が「現行の**共通資産リポジトリ ＋ 各作業リポジトリ**構成」とだけ書かれており、実在する 3 リポ構成が対象に入っているのか分からない
  2. 案C-1 の共有 `.claude` リポジトリは、構造的には **C-BDC そのもの**（ルート＝`.claude/` の中身）である。にもかかわらず「C-BDC をブランチ化して流用する」のか「新規に作る」（§7-1 は「新規リポジトリを作る」）のかが書かれていない
  3. C-BCP が submodule で C-BDC を抱える現行形は §6-2 案C-2 の不採用理由に該当するが、既存キットを指しての記述が無いため、読者は**この記述が自分たちのキットの否定だ**と気づけない
  4. publish-share／check-assets／`v1.0.1` タグといったリリース工程が案C-1 でも要るのか不要になるのかが不明
* **影響**:
  1. 移行を指示された担当者が、既存キットを壊してよいのか併存させるのかを判断できず、**着手できない**
  2. §7-1 に従って新規リポジトリを作ると、C-BDC と内容が重複した 2 つ目の `.claude` 実体が生まれる（＝本書が最も嫌う複製の増加）
  3. 案C-2 不採用の記述が、実質的に既存リリース物の廃止提案であることが伝わらない
  4. 統制ファイル同乗（CR-1）や配布ゲート（CR-2）といった、レーンA が CRITICAL 級の欠陥を潰して作り込んだ安全装置が、案C-1 で引き継がれるのか捨てられるのかが分からない
* **根拠**:
  1. テーマ `CLAUDE.md`「実体の配布キットは別リポジトリ `base-dev-kit-for-cc`（供給元）／`basic_dot_claude`（配布本体）／`basic_cc_project`（雛型ハブ）」および「3リポ v1.0.1: C-BDK `v1.0.1`／C-BDC `24542c1`／C-BCP `a78738b`(submodule=24542c1)」
  2. `02.Marketplace外資産編/Marketplace外資産_開発・テスト_手順書.md` の表「パターンB: `.claude` を submodule 分割」
  3. v2.0 §6-2 案C-2／§7 の対象範囲／§7-1 手順1「新規リポジトリを作る」
  4. v2.0 全文で `C-BDK|C-BDC|C-BCP|publish-share|check-assets` の**ヒット 0 件**

##### U-C3. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **指摘内容に対する同意の程度の根拠**:
  * §7 を「チームの作業リポジトリの移行手順」として書いたが、**本テーマが実際に配布物を持っている以上、その去就こそが移行の主題**である。抽象的な移行手順だけ書いて実在の資産に触れなかったのは設計として不十分
* **判断材料の追加情報**:
  * **3 リポの去就は作業指示者の判断事項である。** レビューイとしては「C-BDC を流用する」案が最も無駄がないと考えるが、リリース済みの配布物の扱いは公開範囲・利用者の有無に依存するため、レビューイが単独で決めるべきではない
  * したがって本件は**判断依頼サマリに載せる**。本書側の対応は「§7-0 に選択肢と推奨を提示する」までとし、確定は作業指示者の態度表明を待つ
  * 指摘 4)（レーンA の安全装置の継承可否）は、レーンA 側の内容を精査しないと書けない。**本書には「要検討」として項目だけ立て、実質の判断は D-13（下流追随タスク）へ送る**のが現実的

##### U-C3. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが指摘を全面的に認めたうえで、確定を作業指示者判断に委ねる線引きを示した。この線引き自体は妥当（リリース済み配布物の廃止・流用はガバナンス上の意思決定）
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成（実質の決定は 1.0 版の作業指示者態度表明へ）

#### U-C3. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §7-1 の前に「§7-0 現行キットとの関係」を置き、(a) 共有 `.claude` リポジトリ＝**C-BDC を流用**（新規作成ではない） (b) C-BCP の submodule 参照は解除し `.gitignore` + clone へ (c) publish-share／check-assets は「配布ゲート」として存続（対象が submodule bump から共有リポ push に変わる）—— のように 3 リポの去就を明記する | **既存資産を捨てずに移行でき、担当者が着手できる**。レーンA の安全装置の継承可否も同時に決まる | レーンA・02 編との突き合わせが要る（本書だけでは閉じない） |
| 2 | §6-2 案C-2 の段落に「これは現行の C-BCP＋C-BDC 構成に該当する」という 1 文を足すにとどめる | 編集量が最小で誤解だけは防げる | **「ではどうするのか」が示されず移行担当者は動けない** |
| 3 | 「既存キットの移行は別タスク」と §8-3 に明記して切り出す | 本書のスコープが明確になる | **v2.0 を読んだ人が移行に着手できない状態が続く** |

1. **修正案1詳細**: §7-1 手順1 の「中身は `.claude/` の中身そのもの」は C-BDC の構造と一致しているので、流用宣言は 1 文で済む。あわせて §7-3 手順1「既存の `.claude/` を退避」に「**submodule の場合は `git submodule deinit` → `git rm` が要る**（単なる `git mv` では外れない）」という分岐を足す

##### U-C3. レビューイからの見解・指摘・補足（修正案）

* **修正案1 を「案の提示」として採用し、確定は作業指示者の態度表明を待つ。** すなわち §7-0 を新設し、(a)(b)(c) を**レビューイの推奨として**書いたうえで、「3 リポの去就は作業指示者判断（クロスレビュー U-C3）」と明記する。
* 修正案1 詳細の `git submodule deinit` 分岐は、判断を待たずに**無条件で追加する**（submodule 構成からの移行はどの選択肢でも発生しうるため）。
* **採用推奨案の拡張提案**: 指摘 4)（レーンA の安全装置）は §7-0 に「**継承の要否は D-13 で判断**」と 1 行置き、§8-3 に D-13 を起票する。本書で判断を装わないため。

##### U-C3. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 「推奨は書くが確定は作業指示者」という分離は、本書が正本として選択肢を示す責任と、意思決定の所在を混同しない点で適切。`git submodule deinit` の無条件追加も妥当
* **最終結論・最終修正案**: §7-0 を新設し (a)(b)(c) をレビューイ推奨として提示 ＋ 作業指示者判断である旨を明記 ＋ §7-3 に submodule 分岐を無条件追加 ＋ §8-3 に D-13 を起票
* **収束ステータス**: 達成（確定は 1.0 版へ）

#### U-C3. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-C4. §5-6 のランチャーが、既存ランチャー（C-BDK `docs/launcher/` 正本）との関係を示さない

**観点**: 下流成果物との整合 ／ 運用に移したときの現実性

#### U-C4. 問題点

* **概要**: §5-6 は `start_claude_code.ps1` を新規スニペットとして提示するが、同名のランチャーは既に設計・実装・3 巡クロスレビュー・リリース済みで、正本は C-BDK `docs/launcher/` にある。両者は責務も構成も別物で、読者は「置き換えるのか・足すのか」を判断できない。
* **問題点**:
  1. 既存ランチャーは 6 ファイル構成（`start_claude_code` ＋ `.claude/launcher/setup-environment` ＋ `option-settings` ＋ `custom.env` ＋各テンプレート）で、§5-6 の 4 責務版には対応物が無い
  2. **ファイル名が完全に一致**（`start_claude_code.ps1`・作業リポジトリのルート配置）しており、既存キット導入済みのチームでは中身だけが別物の同名ファイルが衝突する
  3. **既存ランチャーが担っていた env／秘匿値の受け皿（`custom.env`）が §5-6 から消えている**のに、代替が本書に書かれていない。§5-2 の振り分け表にも env／秘匿の行が無い
  4. §5-6 は PowerShell 版のみで、既存キットが持つ `.sh` 版・改行コード方針への言及が無い
* **影響**:
  1. 既存ランチャーの利用者は、option-settings で管理していた起動オプション群をどこへ移すのか分からない
  2. 同名ファイルの取り違えにより、片方を上書きして機能を失う事故が起きうる
  3. **秘匿値の扱いが退行しうる。** 既存設計は「`settings.json` 系に env を書かせない」ために `custom.env` を用意し、分類A（API キー等）は OS 環境変数へという判定木まで確定させている。§5-6 だけを見て実装すると、行き場を失った env が `.claude/settings.json` へ流れ込み、**しかもそれは共有リポジトリなので全メンバーに配布される**
  4. Windows/macOS 混在チームで `.sh` 版の要否を判断できない
* **詳細**:
  1. 既存ランチャーは Fable クロスレビュー 3 巡＋マージ前セルフレビューを経て `v1.0.1` として 3 リポへ配布済み
  2. §5-6 の責務は「①存在確認 ②ローカル変更の検査 ③同期 ④起動」の 4 つで、**env 設定ステップが無い**
  3. 案C-1 は起動フラグを不要にしたので option-settings の大半（`--add-dir`／`--settings`／`--mcp-config`）は用済みになる。つまり「**既存ランチャーは大幅に単純化できる**」というのが本書の含意のはずだが、明示されていない
* **根拠**:
  1. v2.0 §5-6「責務は 4 つだけ」
  2. `04.資産インベントリ・統合/04.ランチャースクリプト実装/ランチャースクリプト構成設計.md`（正本バナー・6 ファイル構成）
  3. `同/ランチャースクリプト実装計画.md` の配布表（`start_claude_code.{sh,ps1}` → C-BCP ルート／`.claude/launcher/*` → C-BDC）
  4. 同「うっかりセンシティブな環境変数値を settings.json に設定して commit/push してしまったり…**settings.json 系ファイルに env 要素を設定しない習慣を根付かせるための仕組み**」
  5. 同 撤回バナー「**分類A（API キー / 認証トークン / パスワード / 組織外秘の URL）は `custom.env` を含むいかなるランチャーファイルにも書かず、OS の環境変数として設定する**」
  6. v2.0 で `custom.env`・秘匿・env の方針が grep ヒット無し

##### U-C4. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。指摘 3)（秘匿値の退行）は本レビュー全体で最も危険度が高い**と判断する。
* **指摘内容に対する同意の程度の根拠**:
  * §3-4 で「`settings.json` の `env` ブロックは trust 前でも実行される」と自ら書いておきながら、**その `settings.json` が全メンバーへ配布される共有リポに置かれるという案C-1 固有の条件を、秘匿の観点で評価していなかった**
  * 案C-1 では `.claude/settings.json` が「1 人の設定」から「全員に配られる設定」へ性質が変わる。この変化は env の扱いに直結するのに、本書はその変化を明示していない
* **判断材料の追加情報**:
  * **秘匿方針は既存設計（分類A は OS 環境変数）をそのまま維持できる**。案C-1 で変わるのは「`custom.env` を読むステップをランチャーが持つかどうか」であって、秘匿の判定木自体は変わらない
  * 既存ランチャーとの関係（置き換えか追加か）は**作業指示者の判断事項**である。U-C3 と同根（3 リポの去就）なので、判断依頼サマリに載せる

##### U-C4. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが危険度の評価（秘匿退行が最重要）を示し、案C-1 固有の条件変化（`settings.json` の性質が変わる）を言語化した。指摘の成立は揺るがない
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-C4. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §5-6 に「既存ランチャーとの関係」注記を追加し、(a) 正本は C-BDK `docs/launcher/` のまま (b) 案C-1 では起動フラグ層（option-settings）が不要化し本節の 4 責務へ縮退する (c) **env／秘匿は従来方針を維持**（分類A は OS 環境変数、`.claude/settings.json` の `env` に秘匿を置かない）を明記する | **既存資産の去就と秘匿方針が同時に固定でき、退行事故を防げる** | C-BDK 側の設計書改訂を誘発する |
| 2 | §5-2 の振り分け表に「env（非秘匿）／秘匿値」の 2 行を追加する | 表を見るだけで置き場所が分かる。編集が局所的 | 既存ランチャーとの関係は未解決のまま |
| 3 | §5-6 のコード例を「最小例（既存ランチャーがある場合は③の同期ステップだけを既存実装へ差し込む）」と位置づけ直す | 置き換え／追加の二者択一を回避できる | 既存実装を持たない読者にはやや冗長 |

1. **修正案1詳細**: (c) は 1 文で足りる ——「共有 `.claude` リポジトリの `settings.json` は全メンバーへ配布されるので、`env` ブロックに秘匿値を書かない。秘匿値は OS 環境変数（またはランチャーが読む gitignore 済みファイル）に置く」。§3-4 が「`settings.json` の `env` は trust 前でも使われる」と書いている直後に置くと文脈が通る

##### U-C4. レビューイからの見解・指摘・補足（修正案）

* **修正案1 ＋ 修正案2 ＋ 修正案3 をすべて採用する。** 3 案は排他ではなく、それぞれ別の穴を塞ぐため:
  * 修正案2（振り分け表への行追加）は**秘匿値の置き場所**を塞ぐ ← 最優先
  * 修正案1（関係の注記）は**既存資産の去就**を塞ぐ
  * 修正案3（最小例への位置づけ直し）は**同名ファイル衝突**を塞ぐ
* **採用推奨案の拡張提案**: §3-4 の直後に「**案C-1 では `settings.json` の性質が変わる**（1 人の設定 → 全員に配られる設定）。したがって `env` ブロックに秘匿値を置いてはならない」という警告を独立ブロックで置く。振り分け表の 1 行より強く目に入る必要があるため。

##### U-C4. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 3 案がそれぞれ別の問題点（3/1/2）に対応しているというレビューイの整理は正確で、全採用が合理的。§3-4 直後の独立警告は、影響 3（秘匿退行）の重大性に見合う配置
* **最終結論・最終修正案**: 修正案1 ＋ 2 ＋ 3 ＋ §3-4 直後の独立警告ブロック。ただし (a)「既存ランチャーを置き換えるか併存させるか」の確定は作業指示者判断（判断依頼サマリへ）
* **収束ステータス**: 達成（確定は 1.0 版へ）

#### U-C4. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

## IMPORTANT

### U-I1. 案C-1 での開発・テスト手順が空白で、02 編の既存手順（方法B＝`--add-dir`+env+`--settings`）は成立しなくなる

**観点**: 下流成果物との整合 ／ 運用に移したときの現実性

#### U-I1. 問題点

* **概要**: 02 編の本命テスト手段は `--add-dir`＋環境変数＋`--settings` による結合検証だが、案C-1 はこれらをすべて捨てる。メンテナーがブランチの中身を publish 前に検証する手順が、v2.0 にも 02 編にも存在しない状態になる。
* **問題点**:
  1. 02 編の「方法B：正式機能検証・**本命**」が案C-1 では使えない（というより不要になる）のに、その旨がどこにも書かれていない
  2. v2.0 §7-2 にはブランチを切る手順はあるが、**切ったブランチが期待どおりロードされるかを確かめる手順が無い**
  3. 公開前ゲート（check-assets／`/security-review`）が案C-1 のブランチ運用でどう適用されるのかも不明
* **影響**:
  1. 02 編に従う実務者は、v2.0 が不要と断じた 3 点セットを使い続ける
  2. **メンテナーが未検証のブランチを push し、切り替えた全メンバーが壊れた `.claude/` を掴む経路が開く**（消費側は `reset --hard` なのでロールバック手段が「メンテナーが直すまで待つ」しかない）
  3. 公開リポへの内部成果物流出（レーンA CR-2 が潰した CRITICAL）と同種の事故が新しい経路で再発しうる
* **詳細**:
  1. 案C-1 では検証は素直になる —— 検証用の空リポジトリの `.claude/` へ対象ブランチを clone し、ネイティブ起動して `/context` で確認すればよい。つまり 02 編の「方法A（ネイティブ起動スモーク）」が**本命に昇格**し、方法B が不要になる
  2. ブランチ単位で検証対象が変わるため、「`main` を更新したら全ブランチで検証が要るのか」という論点も生じる
* **根拠**:
  1. `02.Marketplace外資産編/Marketplace外資産_開発・テスト_手順書.md`「方法B：正式機能検証・**本命**」
  2. v2.0 §5-1「`--add-dir` / `--settings` / `--mcp-config` / 環境変数はいずれも不要になる」
  3. v2.0 §7-2 はブランチ作成の 4 手順のみで検証ステップを含まない

##### U-I1. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **判断材料の追加情報**:
  * 影響 2 は特に重い。**案C-1 は消費側が `reset --hard` の読み取り専用ミラーなので、メンバー側に自衛手段が無い**。検証ゲートはメンテナー側にしか置けない設計であり、その手順が空白なのは構造的な欠落である
  * 検証手順そのものは既に本書内に材料がある（§3-1 の実測で使った「作業リポの `.claude/` に clone して起動し合言葉を回収する」手法がそのまま検証手順になる）

##### U-I1. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが影響の構造的性質（消費側に自衛手段が無い）を補強し、既存の実測手法が検証手順に流用できることも示した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I1. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §7-2 に手順 5「検証用リポジトリで対象ブランチを clone し、`/context` でロードを確認してから push する」を追加し、02 編の方法A へリンクする | **既存の検証資産を活かしつつ抜けを塞げる**。追記は数行 | 02 編側の「方法B が本命」という記述は残るので、02 編にも 1 行の注記が要る |
| 2 | §7 の末尾に「§7-5 ブランチの検証とゲート」を新設し、check-assets／`/security-review` の適用点まで書く | リリース工程としての完結度が上がる | レーンA との役割分担の整理が要る（スコープが膨らむ） |

1. **修正案1詳細**: U-C1 の修正案（§7-5 既存成果物の扱い）と同じ表の中で「02 編＝方法A が本命に昇格・方法B は案A 用として保存」と 1 行書けば本書側の対応は完了する

##### U-I1. レビューイからの見解・指摘・補足（修正案）

* 修正案1 を採用する。
* **採用推奨案の拡張提案**: 検証手順に「**`main` を更新したら、`main` を取り込んだ各葉ブランチでも検証する**」という条件を 1 行加える。指摘の詳細 2) が提起した論点（`main` 更新時の検証範囲）への回答であり、U-I7（`main` 更新時の定常手順）とも接続する。

##### U-I1. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 拡張提案は詳細 2) の未回答論点を閉じるもので、U-I7 との整合も取れる
* **最終結論・最終修正案**: 修正案1 ＋ `main` 更新時の検証範囲の明記
* **収束ステータス**: 達成

#### U-I1. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I2. §5-6 の非対称（`reset --hard`）が「本リポジトリ」を主語に説明され、チームが設定すべき deny ルールの指示が無い

**観点**: 運用に移したときの現実性

#### U-I2. 問題点

* **概要**: 非対称そのものの説明（permissions は Claude のツール呼び出しにしか効かない）は正確だが、根拠が「**本リポジトリ**の共有 `settings.json` は deny している」という読者が持っていないリポジトリの設定に置かれており、読者側で何を設定すればこの非対称が成立するのかが書かれていない。
* **問題点**:
  1. 「本リポジトリ」が `cc-relative-info`（本書が置かれている調査リポジトリ）を指しており、想定読者（他チームのガバナンス担当者）には該当しない
  2. 共有 `.claude` リポジトリの `settings.json` に `git reset --hard` の deny を入れる指示が §5-2・§7-1 のどこにも無い。**deny が無ければ非対称は成立せず、周知文だけが浮く**
  3. deny は**コマンド文字列に対して**効くため、Claude にランチャースクリプト自体を実行させれば `reset --hard` が走る。**この抜け道への言及が無い**
* **影響**:
  1. 読者が §5-6 の警告を自分たちの設定に翻訳できない
  2. 「Claude には reset --hard をさせない」という前提が設定されないまま周知だけ行われ、実際には Claude が `.claude/` を巻き戻せる状態が残る
  3. 抜け道を知らないまま「deny したから安全」と判断する
* **根拠**:
  1. v2.0 §5-6 の警告ブロック（「**本リポジトリの共有 `settings.json`** は…deny している」）
  2. v2.0 §5-2 の振り分け表・§7-1 の 4 手順に deny 設定の指示なし
  3. `cc-relative-info` ルート `CLAUDE.md` と直近コミット（`git -C 経由の reset --hard も deny で塞ぐ`）—— この deny は調査リポジトリ側の設定であり読者のチームのものではない

##### U-I2. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **指摘内容に対する同意の程度の根拠**:
  * 執筆時、自リポジトリの設定を「実例」として書いたが、**想定読者は他チームのガバナンス担当者**であり、実例のつもりが指示に読めてしまう位置に置いた
* **判断材料の追加情報**:
  * 指摘 3)（ランチャー経由の抜け道）は独立に確認した。permissions はツール呼び出しのコマンド文字列に対して照合されるため、`./start_claude_code.ps1` という 1 コマンドは `reset --hard` のパターンにマッチしない。**指摘は正しい**

##### U-I2. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが執筆意図（実例のつもり）と結果（指示に読める）のずれを認め、抜け道の成立も独立に確認した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I2. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | 「本リポジトリの共有 `settings.json` は…」を「**共有 `.claude` リポジトリの `settings.json` に次の deny を入れる**」へ書き換え、deny の JSON 断片（2 パターン）を示す。あわせて §7-1 の手順に「deny を設定する」を追加 | **読者がコピーして設定できる。周知と設定が同時に閉じる** | 具体パターンを載せると CLI の版差で陳腐化しうる（§8-1 の版依存注記でカバー可能） |
| 2 | 現文のまま「（本書が置かれている調査リポジトリでの実例）」と補足する | 最小の編集で誤読は減る | **読者が自分の設定に翻訳する作業が残る** |

1. **修正案1詳細**: 抜け道は §5-7 に「**ランチャーを Claude に実行させない**（deny はコマンド文字列に効くので、スクリプト経由なら通る）」の 1 行を足せばよい

##### U-I2. レビューイからの見解・指摘・補足（修正案）

* 修正案1 を採用する。
* **採用推奨案の拡張提案**: deny の JSON 断片は**共有リポの `settings.json` に置く前提**で書く。加えて、論理整合性観点 C2 で判明した「**`deny` は trust 非依存で必ず効く／`allow` は trust 依存**」（BASE-D L81153）を同じ箇所に併記する。**この deny が案C-1 で確実に機能することの根拠**になるため。

##### U-I2. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: `deny` が trust 非依存であることの併記は、本指摘の対処（deny を設定させる）に「それが確実に効く」という裏づけを与えるもので、C2 の発見を建設的に活用している
* **最終結論・最終修正案**: 修正案1 ＋ §5-7 への抜け道注記 ＋ `deny` の trust 非依存性の併記
* **収束ステータス**: 達成

#### U-I2. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I3. §8-2 の未検証項目に「誰が・いつ確認するか」と「案C-1 の採否をブロックするか」が無い

**観点**: 読み手の意思決定を支えるか

#### U-I3. 問題点

* **概要**: V1〜V6 は列挙されているが、影響列が「〜が変わる」といった記述にとどまり、読者が「今すぐ採用を決めてよいのか、確認を待つべきか」を判断できない。
* **問題点**:
  1. 各項目に**担当・期限・確認方法**が無い。V1 は claude.ai の組織設定画面を見るだけで数分で決着するのに未検証のまま残っている
  2. 「ブロックする／しない」の明示が無い。V1 は本書自身の論理からは**ブロックしない**（`CLAUDE.md`/`rules`/`permissions` は plugin で運べないので V1 の結果に関わらず P1 は要る）はずだが、そう書かれていない
  3. 逆に **V6 は §5-5・§7-4 の中核前提**に直結するのに、影響列は「読み込み位置の想定が特に重要」という曖昧な表現
* **影響**:
  1. 慎重な読者は V1 の結果待ちで採用判断を止める。積極的な読者は V6 未検証のまま全社展開し、Windows で個人設定が読まれない事故に当たる
  2. 未検証項目が誰の宿題でもないまま放置される（実際 V1〜V4 は骨子の段階から同じ内容で持ち越されている）
  3. **§7-4 の周知文「個人設定は `.claude/settings.local.json` に書く。ここだけは消えない」が、V6 の結果次第で虚偽になる**
* **根拠**:
  1. v2.0 §8-2 の表（列は「# ／ 内容 ／ 案C-1 への影響」の 3 列のみ）
  2. v2.0 §7-4 の断定形の周知文
  3. `drafts/v2.0_章立て骨子.md` に同内容の未処理項目が既出

##### U-I3. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **判断材料の追加情報**:
  * 論理整合性観点 S6 も V6 について同じ指摘をしており、さらに「**V6 の残リスクは入れ子リポジトリがリポジトリルート判定に影響するかの一点に絞り込める**」という分析を加えている
  * 同観点 C2 で新設される V7（入れ子リポジトリと trust）と V6 は**同根**なので、統合して扱う
  * 指摘 3)（周知文が虚偽になりうる）は §7-4 の記述として直す必要がある

##### U-I3. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが別観点の分析と統合し、V6/V7 を同根として扱う方針を示した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I3. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §8-2 の表に「**採否をブロックするか**」「担当」「確認方法」の 3 列を足し、V1＝ブロックしない／V6・V7＝**展開前に必須**と区分する | **読者が「今決めてよい」と確信でき、残タスクにも持ち主が付く** | 担当は本書執筆時点で未定の可能性（空欄でも「要アサイン」と分かるので可） |
| 2 | §0 に「未検証項目は採否をブロックしない（ただし V6 は展開前に確認）」の 1 行を置く | 最短で意思決定を後押しできる | §8-2 の粒度は改善されない |
| 3 | §7-4 の周知文を条件付きにする | 虚偽周知のリスクが消える | 周知文の歯切れが悪くなる |

##### U-I3. レビューイからの見解・指摘・補足（修正案）

* **修正案1 ＋ 修正案3 を採用する。** 修正案2 は §0 の簡潔さを損なう割に §8-2 の粒度問題が残るため採らない。
* **採用推奨案の拡張提案**: §7-4 の周知文は「無くならない（git の性質）」と「効く（ロードされる）」を**分けて書く**。前者は実測済みで断定してよく、後者だけが V6/V7 待ちである。条件付きにして全体を曖昧にするより、確定部分は確定と書くほうが周知として強い。

##### U-I3. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 「消えないこと」と「効くこと」を分ける整理は、S6 が指摘した区別（担保されているのは前者のみ）と一致し、周知文の実効性を保ちながら虚偽を避ける
* **最終結論・最終修正案**: 修正案1 ＋ 修正案3（「消えない」と「効く」を分けて書く形で）
* **収束ステータス**: 達成

#### U-I3. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I4. 「素の `claude` でも deny が効く」という優位性は、`.claude/` が未 clone の状態では成立しない

**観点**: 読み手の意思決定を支えるか ／ 運用に移したときの現実性

#### U-I4. 問題点

* **概要**: §6-1 の比較表は案C-1 の優位点として「素の `claude` 起動での `deny`：効く」を挙げるが、案C-1 では `.claude/` が作業リポジトリから gitignore されるため、**新規メンバーが clone した直後は `.claude/` が存在しない**。
* **問題点**:
  1. §6-1 の「効く」が無条件に読める。現状維持（`.claude/` を commit する案）とは前提が異なるのに同じ「効く」で並んでいる
  2. §7-4 の周知「素の `claude` でも動くが、`.claude/` が**古いまま**使われる」は初回のケースを取りこぼしている（古いのではなく**無い**）
  3. `.claude/` が無い状態を検知して警告する仕組みが §5-7 の注意リストに無い
* **影響**:
  1. 比較表を根拠に「案C-1 ならランチャーを強制しなくても統制が効く」と判断すると、**オンボーディング直後の最も事故りやすい時期にガードがゼロになる**
  2. 新規メンバーが「clone したので使える」と思って素の `claude` を起動し、チーム規約なしで作業する
  3. `.claude/` が無いことに誰も気づかない（statusline にブランチを出す策は `.claude/` があって初めて機能する）
* **詳細**:
  1. 既存キット（案A 系）は `--add-dir` 前提なので「ランチャー経由でなければ効かない」ことが自明だったが、案C-1 は「実体があるから効く」という設計なので、**実体が無い状態という新しい失敗モードが生まれている**
* **根拠**:
  1. v2.0 §6-1 の deny 行／§5-7・§7-3 の gitignore 指示／§7-4 の周知文／§5-6 手順1（clone するだけで通知しない）

##### U-I4. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **判断材料の追加情報**:
  * 論理整合性観点 I1 も §6-1 の同じセルについて別の条件（リポジトリルートから起動した場合に限る）を指摘している。**同じセルに 2 つの条件が付く**ことになるので、まとめて書き直す
  * 結果として §6-1 の該当セルは「効く（**`.claude/` が clone 済みで、リポジトリルートから起動した場合**）」となる

##### U-I4. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが別観点の指摘と統合し、該当セルの最終形を示した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I4. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §6-1 の該当セルを条件付きに改め、§7-4 の周知文を「初回は `.claude/` が**存在しない**。必ずランチャーで初期化してから使う」に直す | **比較表の誇張が消え、最も事故りやすい時期に手当てが入る** | 案C-1 の優位性の見え方がわずかに弱まる |
| 2 | 作業リポジトリのルート `CLAUDE.md` に「`.claude/` が無い状態で作業を始めない」の記載を入れることを §5-2／§7-3 で推奨する | commit される側に載るので必ず届く | 定型文が各リポに複製される |
| 3 | 1 ＋ 2 の両方 | 表示・周知・自動の 3 重で塞げる | 記述箇所が増える |

##### U-I4. レビューイからの見解・指摘・補足（修正案）

* **修正案3 を採用する。** 修正案2 の「定型文が各リポに複製される」というデメリットは、**1 行の定型文であれば許容範囲**と判断する（本書が問題視している複製は「共通資産の実体」であって、リポジトリ固有ファイルへの 1 行の注意書きではない）。

##### U-I4. レビューア確認: 修正案（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイの「複製の対象が違う」という区別は妥当で、修正案3 のデメリットが実質的に小さいことを示している
* **最終結論・最終修正案**: 修正案3
* **収束ステータス**: 達成

#### U-I4. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I5. 節間リンクの表示ラベルと着地点が一致せず、976 行の中で目的の小節へ飛べない

**観点**: 読みやすさ・たどりやすさ

#### U-I5. 問題点

* **概要**: `§5-6` と書かれたリンクが §5 の冒頭や §5-7 に着地するなど、ラベルと飛び先が合わないリンクが複数ある。
* **問題点**:
  1. **ラベル誤り**: §3-4 末尾「（[§5-6](#cautions)）」の `#cautions` は §5-7 のアンカー。内容は §5-7 側にあるのでラベルの方が誤り
  2. **親アンカー着地**: §7-2・§7-3・§7-4 のリンクはいずれも §5 の見出しに着地する
  3. §5-7・§6-1 等も `#control`（§4 冒頭）で §4-1／§4-3 を指す
  4. §5-2〜§5-7 のうちアンカーを持つのは 3 つだけで、付与基準が読み取れない
* **影響**:
  1. §3-4 のリンクを辿った読者は「§5-6 にあるはずの記述」を §5-7 で見つけることになる
  2. **移行手順（§7）は他節への参照が多いので、移行担当者が最も往復する箇所で最も効率が悪い**
  3. §0「最初に読む場所」表は良くできているが、本文中の相互参照がこの水準に達していない
* **根拠**:
  1. v2.0 §3-4 末尾のリンクとアンカー定義位置／§7-2〜§7-4・§5-7・§6-1 の各リンク

##### U-I5. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **判断材料の追加情報**:
  * **実用性観点 I-5・論理整合性観点 S3 も同じ指摘をしており、3 観点すべてが一致している**。優先度は高い
  * アンカーの付与基準が読み取れないのは、**執筆時に「他成果物から参照されるアンカー（`#matrix`・`#appendix-a` 等）」の互換維持を優先し、内部リンク用のアンカーを後回しにした**ため

##### U-I5. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: 3 観点一致の指摘であり、レビューイが付与基準の不統一の経緯も説明した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I5. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | 全小節（§x-y）にアンカーを付与し、本文中のリンクを小節アンカーへ張り替える。§3-4 の「[§5-6]」は「[§5-7]」へラベル修正 | **976 行を往復する読者のコストが最も下がる**。機械的に実施でき内容変更を伴わない | アンカー定義行が 10 行ほど増える |
| 2 | ラベル誤りのみ直す | 最小コスト | 親アンカー着地は残る |

##### U-I5. レビューイからの見解・指摘・補足（修正案）

* 修正案1 を採用する。
* **採用推奨案の拡張提案**: アンカー名の命名規則を決める（`#s5-3` のように節番号ベース）。既存の意味ベースのアンカー（`#mirror`・`#cautions` 等）は**他成果物からの参照互換のため残し**、節番号ベースのものを追加する二重付与とする。節番号は改訂で動きうるが、動いたときに機械的に直せる利点が上回る。

##### U-I5. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 二重付与により参照互換と内部リンクの両立が図れる。命名規則の明示は今後の改訂での一貫性を担保する
* **最終結論・最終修正案**: 修正案1 ＋ 節番号ベースのアンカーを追加する二重付与（既存の意味ベースアンカーは互換のため保持）
* **収束ステータス**: 達成

#### U-I5. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I6. 「実測記録の再現手順」にスクリプトが無く、「CLI の版が上がったら測り直せ」という指示を実行できない

**観点**: 運用に移したときの現実性

#### U-I6. 問題点

* **概要**: 末尾の節は「本書の【実測】は**次のスクリプトで再現できる**」と宣言するが、続くのは手法の要約表だけでスクリプトは存在しない。
* **問題点**:
  1. 「次のスクリプトで再現できる」という記述と実体（表のみ）が一致しない
  2. 表の「内容」列は手法の要約で、**再現に必要な具体**（合言葉の埋め方・許可ツールの絞り方・判定方法）が §3-1／§3-2 の引用ブロックに散在している
  3. 再測の成否を判定する基準が集約されていない
* **影響**:
  1. 版が上がったときに再測が行われず、**本書の中核主張が検証されないまま古びる**
  2. 再測を試みる人が複数節を読み直して手法を再構成する羽目になる
  3. 本書は「案C-1 は公式にサポートされた構成ではない」と正しく断っているので、**再測こそが唯一の安全弁**だが、その安全弁が動かない
* **根拠**:
  1. v2.0「実測記録の再現手順」節の宣言と実体／§8-1「CLI の版が上がったら再測すること」

##### U-I6. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。** 「次のスクリプトで」という文言は明確な誤りである。
* **判断材料の追加情報**:
  * 実際の検証スクリプトはセッションのスクラッチパッドに存在するが、**リポジトリには置いていない**。再現性を謳う以上、実体をリポジトリに置くべきだった

##### U-I6. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが文言の誤りを認め、スクリプト実体がリポジトリ外にあることを確認した
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I6. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | 表の各行に「再現コマンド」列を足し、そのまま実行できる形を書く | **再測が実際に回る。本書内完結の方針とも整合** | 本書がさらに長くなる |
| 2 | スクリプト実体を別フォルダに置き本節からリンクする | 本文を膨らませずに再現性を担保できる | 「本書内に保持する」執筆方針から外れる。リンク切れリスク |
| 3 | 文言を「次の手法で再現できる」に直すだけ | 虚偽記述は消える。編集 1 語 | **再測の実行可能性は改善しない** |

##### U-I6. レビューイからの見解・指摘・補足（修正案）

* **修正案1 ＋ 修正案2 を採用する。** 本文には再現の要点（何をどう判定するか）を書き（修正案1）、実行可能なスクリプト実体は `verification/` 配下へ置いてリンクする（修正案2）。
* **採用推奨案の拡張提案**: 「本書内に保持する」執筆方針との関係を明記する。**この方針は「判断に必要な情報」に適用されるものであって、実行可能なスクリプトの実体まで本文に埋め込む趣旨ではない**。方針の射程を 1 行で断っておけば、修正案2 のデメリットは解消する。

##### U-I6. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 執筆方針の射程を明示するというレビューイの整理により、修正案2 の唯一のデメリット（方針との不整合）が解消される。両採用が最善
* **最終結論・最終修正案**: 修正案1 ＋ 修正案2 ＋ 執筆方針の射程の明記
* **収束ステータス**: 達成

#### U-I6. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-I7. `main` 更新時の定常作業手順が 1 系列の例示だけで、複数系列での追従手順が示されていない

**観点**: 運用に移したときの現実性

#### U-I7. 問題点

* **概要**: §5-4 は `rebase.updateRefs` を使った 1 系列の例を示すが、§5-1 の構成図には `java/*` と `python/*` の 2 系列がある。`main` を 1 回更新したときにメンテナーが実行すべき全手順が書かれていない。
* **問題点**:
  1. `--update-refs` が一括更新するのは「rebase 対象コミットを指す**中間**ブランチ」であり、**別系列は動かない**。この制約が明記されていない
  2. 「`main` を更新したら何をするか」という**定常作業のチェックリスト**が無い
  3. §5-4 は「運用に組み込むならスクリプト化する」と書くが、対象が fetch と追跡ブランチ作成に限定されており、**rebase ループ自体は対象外**
* **影響**:
  1. 系列が 2 つ以上あるチームで、片方の系列だけが `main` を取り込み、もう片方が取り残される。しかも §5-4 の売り（グラフで反映漏れが見える）は**見に行かないと分からない**
  2. メンテナー 1〜2 名の属人作業になり、交代手順を書いても実際の手順が引き継げない
  3. 取り残されたブランチを使っているメンバーは、`main` に入ったチーム規約（例: 新しい deny）が効かないまま作業する
* **詳細**:
  1. 必要なのは「葉ブランチを列挙し、各葉で `git -c rebase.updateRefs=true rebase main` を実行し、最後にまとめて force-push する」という定型と、「取り込み済みかの確認は `git branch --no-merged main` で一覧できる」という検証手段
* **根拠**:
  1. v2.0 §5-4 のコマンド例（`java` 系列のみ）／§5-1 の構成図（2 系列）／§5-4 の警告（rebase の反復には言及なし）

##### U-I7. レビューイからの回答・見解・補足（問題点）

* **指摘内容に対する同意の程度**: **全面的に同意する。**
* **判断材料の追加情報**:
  * レビューアが挙げた `git branch --no-merged main` は**取り残し検知の決定的な手段**である。§5-4 は「グラフで視覚的に把握できる」を rebase 採用の第一の理由に挙げているが、**視覚的確認は人が見に行かないと機能しない**。機械的に確認できるコマンドを併記すれば、rebase 方針の利点を運用で担保できる

##### U-I7. レビューア確認: 問題点（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが指摘を認め、`--no-merged` が rebase 方針の利点を運用で担保する手段になるという補強を加えた
* **最終結論**: 元の指摘確定
* **収束ステータス**: 達成

#### U-I7. 修正案

| # | 修正案 | メリット | デメリット |
|---|---|---|---|
| ★1 | §5-4 に「`main` 更新時の定常手順」を 4 ステップの箇条書きで追加（①全ブランチを fetch＋ローカル追跡 ②**各系列の葉ブランチごとに** rebase ③`git branch --no-merged main` が空であることを確認 ④まとめて force-push） | **手順が引き継げる形になり、取り残しを機械的に検知できる** | §5-4 が少し長くなる |
| 2 | `--update-refs` の効き方に「同一系列内のみ。別系列は別途 rebase が要る」の 1 文を足す | 誤解だけは防げる。1 行 | **定常手順は依然として読者が組み立てる** |

##### U-I7. レビューイからの見解・指摘・補足（修正案）

* 修正案1 を採用する（修正案2 の 1 文も修正案1 の中に含める）。
* **採用推奨案の拡張提案**: ステップ③の `git branch --no-merged main` は **U-I1 の検証手順（ブランチを push する前に検証する）とセットで実行する**位置づけにする。「取り込み漏れがない」ことと「取り込んだ結果が正しくロードされる」ことは別の確認であり、両方を定常手順に含める。

##### U-I7. レビューア確認: 修正案（0.9）

* **レビューア態度**: 修正採用
* **理由**: 2 種類の確認（漏れの検知／結果の検証）を定常手順に併置する整理は、U-I1 との重複を避けつつ両者を接続する
* **最終結論・最終修正案**: 修正案1（修正案2 を包含）＋ U-I1 の検証手順との併置
* **収束ステータス**: 達成

#### U-I7. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

## SUGGESTION

### U-S1. 役割別の読み始め導線があると、チーム展開時の配布が楽になる

**観点**: 読みやすさ・たどりやすさ

#### U-S1. 観点

* **概要**: §0「最初に読む場所」は「知りたいこと」軸で良くできているが、**役割**軸（意思決定者／メンテナー／一般メンバー）の導線があると、そのまま「この節だけ読んで」と配れる。
* **問題点**: 1) 一般メンバーに必要なのは §7-4 の 5 行だけだが、そこへ直行する導線が無い 2) メンテナー（1〜2 名）が読むべき範囲が束ねられていない
* **影響**: 1) 976 行を全員に配ることになり周知の実効性が落ちる 2) メンテナーの引き継ぎ時に読む範囲を都度説明する必要がある
* **根拠**: v2.0 §0「最初に読む場所」表（すべて「知りたいこと」軸）／§7-4（独立して配布可能な粒度）

#### U-S1. 提案

1. **提案1**: §0 の表に役割行を 3 行追加する（「チームメンバーに配る 5 行 → §7-4」「メンテナーの担当範囲 → §5-3〜§5-5・§7-1〜§7-2」「意思決定者 → §0・§6-1・§8-2」）
2. **提案2**: §7-4 を独立ファイルとして切り出す（ただし二重管理になる）

#### U-S1. レビューイからの見解・補足

* 提案1 を採用する。提案2 は二重管理になるため採らない。表の軸が混在する点は、見出しで「知りたいことで選ぶ」「役割で選ぶ」と分けることで解消する。

#### U-S1. レビューア確認（0.9）

* **レビューア態度**: 受容
* **理由**: レビューイが提案1 を採用し、軸の混在というデメリットへの対処も示した
* **最終結論・最終提案**: 提案1（見出しで軸を分ける形で）
* **収束ステータス**: 達成

#### U-S1. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-S2. 案の記号体系（案A／案A'／案B／案C-1／案C-2／案D）の由来が本書内で説明されていない

**観点**: 読みやすさ・たどりやすさ

#### U-S2. 観点

* **概要**: v2.0 は v1.2 の案番号を引き継いでいるが、その旨も、案C-1 の「-1」が何に対する枝分かれかも本書には書かれていない。
* **問題点**: 1) 「案C」が本書に登場せず、いきなり案C-1／案C-2 が出る 2) 案A' が D-11 の別名として現れるが、`D-n` という識別子の出所が本書内に無い
* **影響**: 1) v1.2 を読んでいない読者は記号を照合できず §6 の比較を追いにくい 2) `D-11`・`D-5`・`D-12` 等が由来不明の参照として残る
* **根拠**: v2.0 §6-2 の各案名（定義なし）／§8-3 の `D-n`

#### U-S2. 提案

1. **提案1**: §6 冒頭の注記に記号の由来 1 文を足す
2. **提案2**: §6-1 の比較表に v1.2 の対応節へのリンク列を足す

#### U-S2. レビューイからの見解・補足

* 提案1 を採用する。**論理整合性観点 I2 が「案ラベルが v1.2 と衝突している（v2.0 の案A は v1.2 の案C）」という、より重い事実誤認を指摘している**ため、I2 の対応（§6 冒頭に対応表を置く）に統合する。対応表に `D-n` の由来も 1 行加えれば、本指摘は同時に解消する。

#### U-S2. レビューア確認（0.9）

* **レビューア態度**: 受容
* **理由**: 同一箇所への指摘を 1 つの対応表に統合するのは合理的。記述の分散を避けられる
* **最終結論・最終提案**: 論理整合性観点 I2 の対応表に統合（`D-n` の由来を含める）
* **収束ステータス**: 達成

#### U-S2. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-S3. §5-2「リポジトリ固有の機能資産は plugin」に、install が各自任せになる制約へのリンクが無い

**観点**: 読み手の意思決定を支えるか

#### U-S3. 観点

* **概要**: §5-2 は plugin を推奨するが、v2.1.195 以降「marketplace を宣言しても外部ソースの plugin はメンバーが `claude plugin install` するまでロードされない」という重い制約は §6-2 にしか書かれていない。
* **問題点**: 1) §5-2 の表を根拠に設計すると「clone すれば即動く」と誤解する 2) §6-2 は「代替案と不採用理由」の節なので、採用方針を読んでいる読者は通過しやすい
* **影響**: 1) オンボーディング手順に install を組み込み損ねる 2) 案C-1 の売りである「起動フラグ不要・clone だけで揃う」が plugin 部分については成立しないことに気づかない
* **根拠**: v2.0 §5-2 の plugin 行（install の制約に触れていない）／§6-2 の該当記述

#### U-S3. 提案

1. **提案1**: §5-2 の plugin 行の「理由」欄末尾に「（ただしメンバーの `claude plugin install` が要る＝§6-2）」を足す

#### U-S3. レビューイからの見解・補足

* 全面的に同意する。提案1 を採用する。
* あわせて、**D-12 の調査で判明した制約**（`allowManagedHooksOnly` を敷いた組織では plugin の hooks も原則ブロックされ、managed が force-enable した plugin だけが例外。BASE-D L89557）も同じ行に注記する。plugin へ寄せる判断が配布先の層3 設定に依存することを示すため。

#### U-S3. レビューア確認（0.9）

* **レビューア態度**: 修正採用
* **理由**: レビューイの追加注記は、plugin 推奨の適用条件を示すもので、本指摘（plugin 推奨の制約を見えるようにする）の趣旨を強化する
* **最終結論・最終提案**: 提案1 ＋ `allowManagedHooksOnly` 下での plugin hooks 制約の注記
* **収束ステータス**: 達成

#### U-S3. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-S4. §5-5「ローカル変更があると `switch` の時点で失敗する」は条件付きでしか成り立たない

**観点**: 運用に移したときの現実性

#### U-S4. 観点

* **概要**: `git switch` がダーティなツリーで失敗するのは、切り替え先ブランチで**当該ファイルの内容が異なる**場合に限られる。同一内容の場合は成功し、続く `reset --hard` がローカル変更を破棄する。
* **問題点**: 1) 「`reset --hard` まで到達しない」と断定しており、`switch` が安全装置として機能するかのように読める 2) 本書自身は正しい対策（`switch` より前に `status --porcelain` を検査）を書いているので、記述と対策の根拠がずれている
* **影響**: 1) この文だけを読んでランチャーを実装した人が事前検査を省略し、ローカル変更が黙って破棄される 2) 「壊れはせず止まるだけ」という §5-5 全体の安心感が実際より強く伝わる
* **詳細**: **本所見はツール制約（Bash 不使用）により再実測しておらず、git の文書化された挙動に基づく推測を含む指摘である**
* **根拠**: v2.0 §5-5 の該当文／§5-6 のランチャー実装（正しく事前検査している）

#### U-S4. 提案

1. **提案1**: 断定を条件付きに直し、「`switch` を安全装置にしない」と明記する
2. **提案2**: 実測で条件を確定してから記述を更新する

#### U-S4. レビューイからの見解・補足

* 全面的に同意する。**論理整合性観点 I4 も同じ箇所を独立に指摘しており、2 名のレビューアが一致している**。
* 提案1 を採用する（I4 の修正案1 と同一内容）。記述は「ローカル変更があると `switch` が失敗することがあり、成功した場合も直後の `reset --hard` で失われる。どちらも避けるため switch 前に検査する」とする。

#### U-S4. レビューア確認（0.9）

* **レビューア態度**: 受容
* **理由**: 2 観点一致の指摘であり、レビューイが両者に共通する修正文言を確定した
* **最終結論・最終提案**: 提案1（論理整合性観点 I4 と統合）
* **収束ステータス**: 達成

#### U-S4. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

### U-S5. §4-1 の `--safe-mode` 行の出典 `[10]` が、その行の主張を支えていない

**観点**: 読み手の意思決定を支えるか

#### U-S5. 観点

* **概要**: §4-1 の `--safe-mode` 行は 9 種の資産を一括無効化すると述べて `[10]` を付すが、`[10]` は skills ページの「追加ディレクトリからのロードは `--safe-mode` 等が制限する」という別主張。全量の根拠は `[34]`（cli-reference）である。
* **問題点**: 1) 読者が根拠を辿ると主張と一致しない箇所に着地する 2) 同じ表の bare mode 行は `[36][20]` と適切に併記されており扱いが不揃い
* **影響**: 1) 本書の強み（各主張に出典アンカー）がこの行では機能しない 2) 再照合時に無駄な調査が発生する
* **詳細**: 付録B B-3 の `--safe-mode` 行は正しく `[34]` を付けているので、§4-1 側だけの取りこぼし
* **根拠**: v2.0 §4-1 の `--safe-mode` 行／[10] の定義／[34] の定義／付録B B-3

#### U-S5. 提案

1. **提案1**: §4-1 の `--safe-mode` 行の出典を `[34]` に差し替える（`[10]` を併記してもよい）

#### U-S5. レビューイからの見解・補足

* 全面的に同意する。提案1 を採用し、`[34][10]` の併記とする（`[10]` も「追加ディレクトリからのロードを制限する」という部分主張は支えているため）。

#### U-S5. レビューア確認（0.9）

* **レビューア態度**: 受容
* **理由**: 併記はどちらの主張も支える形になり、bare mode 行との扱いも揃う
* **最終結論・最終提案**: 提案1（`[34][10]` の併記）
* **収束ステータス**: 達成

#### U-S5. 作業指示者の態度表明

* **態度**: （採用 / 条件付き採用 / 差戻し / 保留 / 却下）
* **コメント**:

---

## POSITIVE

### U-P1. §3-2 の実測設計が、誤判定の罠まで含めて記録されている

**観点**: 運用に移したときの現実性

#### U-P1. 評価点

* **概要**: 発火契機の検証で対照群を置き、先行操作ツールだけを許可して合言葉を読めなくし、skill は自己申告でなく `Skill` ツールの起動成否で判定している。さらに「モデルが『ロードされている』と作文した」「`--debug` にはログが出ない」という踏んだ罠まで書いてある。
* **詳細**: 本書の中核主張は設計判断の土台なので、ここが甘いと全体が崩れる。判定方法を機能的確認に切り替えた経緯まで残っているため、読者が結論の信頼度を自分で評価できる。同種の検証をする人にとって再利用価値が高い。
* **根拠**: v2.0 §3-2 の手法と「判定を誤りやすい点」

#### U-P1. レビューイからの見解・補足

* **評価点の認識一致**: ○ 概ね一致
* **追加コメント**: ただし U-I6 で指摘されたとおり、**スクリプト実体がリポジトリに無いため「再利用できる」状態にはなっていない**。U-I6 の対応（`verification/` へスクリプトを置く）で実際に再利用可能にする。
* **強化・横展開の意向**: 「機能的確認で判定する」原則は、論理整合性観点 C2 が提起した V7（trust 依存部分の検証）の再現手順にも適用する。

#### U-P1. 作業指示者の態度表明

* **態度**: （維持 / その他）
* **コメント**:

---

### U-P2. §5-3 の D/F コンフリクトは、チームが 100% 踏む罠を先回りで潰している

**観点**: 運用に移したときの現実性

#### U-P2. 評価点

* **概要**: `java` と `java/design` が git の ref 仕様上共存できないことを実測で示し、「すべてのブランチ名を葉トークンで終える」という規約と対照表に落としている。
* **詳細**: ブランチで作業種別を切り替える設計を採れば `java` と `java/design` を同時に作ろうとするのは自然な発想であり、必ず誰かが踏む。エラーメッセージ原文まで載せているので、遭遇した人が検索で本節に戻れる。`base` を予約語として固定し、アルファベット順で先頭に並ぶという副次的利点まで説明しているのも実務的。
* **根拠**: v2.0 §5-3

#### U-P2. レビューイからの見解・補足

* **評価点の認識一致**: ◎ 一致
* **追加コメント**: この規約（`<group>/base`）は作業指示者の提案によるもの。レビューイの初案は `<group>/_base` だった。
* **強化・横展開の意向**: 無し

#### U-P2. 作業指示者の態度表明

* **態度**: （維持 / その他）
* **コメント**:

---

### U-P3. §5-5 が「個人設定が消える条件」を切り分け、致命的な 1 ケースを警告に昇格させている

**観点**: 運用に移したときの現実性

#### U-P3. 評価点

* **概要**: `reset --hard` 後に `settings.local.json` が残るのは **untracked かつ ignore 済みのとき**だけで、共有リポで 1 度でも commit されると上書きされる、という切り分けを表で示し、「共有リポジトリの `.gitignore` に必ず入れること」を独立した警告にしている。
* **詳細**: 「どこか 1 ブランチにでも commit された瞬間、次の `switch` で全メンバーの個人設定が消える」という影響範囲の書き方が的確。人が間違えやすい箇所と、その影響が全メンバーに及ぶ非対称性の両方を捉えている。
* **根拠**: v2.0 §5-5 の保護表と警告

#### U-P3. レビューイからの見解・補足

* **評価点の認識一致**: ○ 概ね一致
* **追加コメント**: ただし論理整合性観点 S6 が指摘するとおり、**担保できているのは「消えない」ことだけで、「効く（ロードされる）」ことは未検証**である。U-I3 の対応で両者を分けて書く。
* **強化・横展開の意向**: 上記のとおり。

#### U-P3. 作業指示者の態度表明

* **態度**: （維持 / その他）
* **コメント**:

---

### U-P4. §0-1「なぜ書き直すのか」が冒頭にあり、旧版の扱いまで指定している

**観点**: 読みやすさ・たどりやすさ

#### U-P4. 評価点

* **概要**: v1.2 を増改築しない理由を 2 つの崩れた前提として提示し、「v1.2 を削除しない理由」と「**設計判断の根拠には v2.0 を使うこと**」を同じブロックに書いている。
* **詳細**: 版が並存する成果物で読者が最初に抱く「どっちを読めばいいのか」に、本文を読み始める前に答えている。旧版のリンク・出典が生きていることまで説明しており、v1.2 を参照している他成果物の読者にも配慮がある。
* **根拠**: v2.0 §0-1

#### U-P4. レビューイからの見解・補足

* **評価点の認識一致**: △ 部分的一致
* **追加コメント**: 方針の明示は評価いただいたとおりだが、**U-C2 で指摘されたとおり「配慮が下流に届いていない」（下流 5 文書は v1.2 を指したまま）うえ、「凍結された時点記録」という記述自体が事実と異なる**。方針は正しいが実態が伴っていない。
* **強化・横展開の意向**: U-C2 の対応（v1.2 へのバナー設置・下流 5 文書の張り替え・v2.0 側での従属文書の名指し）で実態を方針に合わせる。

#### U-P4. 作業指示者の態度表明

* **態度**: （維持 / その他）
* **コメント**:

---

### U-P5. §4-3 が「無視される」と「起動しなくなる」を区別し、案C-1 の優位点に接続している

**観点**: 読み手の意思決定を支えるか

#### U-P5. 評価点

* **概要**: 層3 を敷いた配布先で `disableSideloadFlags` / `managed-mcp.json` が起動フラグを**拒否して exit させる** 2 経路を表にし、「案C-1 は起動フラグを使わないのでこの罠を踏まない」と設計判断に直結させている。
* **詳細**: 統制機構の議論は「設定が効く/効かない」で終わりがちだが、ここでは失敗モードが「静かに無視」ではなく「セッションが落ちる」であることを明示している。配布先の組織構成を自チームが制御できない状況で、どの方式が壊れにくいかという実務的な判断軸を提供している。
* **根拠**: v2.0 §4-3

#### U-P5. レビューイからの見解・補足

* **評価点の認識一致**: ◎ 一致
* **追加コメント**: 無し
* **強化・横展開の意向**: 同じ「配布先の組織構成に依存する制約」として、U-S3 で追記する `allowManagedHooksOnly` 下の plugin hooks 制約（BASE-D L89557）も §4-3 の近傍に置くことを検討する。

#### U-P5. 作業指示者の態度表明

* **態度**: （維持 / その他）
* **コメント**:

---

## Devil's Advocate チェックリスト

| 主要指摘・結論 | 最強反論の記述 | 状態 |
|---|---|---|
| U-C1 03 テンプレートの取り残し | 「03 は v1.2 の実装物として時点記録扱いにすればよく、v2.0 が言及する義務はない」→ しかし 03 には凍結バナーが無く、テーマ索引からも現役の成果物として案内されている。時点記録にするならその宣言自体が必要であり、どちらにせよ本書か 03 側に記述が要る | 記述あり |
| U-C2 正本の二重化 | 「v2.0 が正本だと本書に書いてあるので、下流が追随すべきであって本書の責任ではない」→ しかし v1.2 側も正本を名乗り続けており、下流は v1.2 を名指しで参照している。**両方が正本を名乗る状態を作ったのは v2.0 側**であり、解消の責任も v2.0 側にある | 記述あり |
| U-C3 既存キットの去就 | 「配布キットの扱いはリリース管理の話で、設計方針書のスコープ外」→ しかし §7 は「移行手順」を掲げており、移行元に実在する構成が含まれるかどうかは手順書の基本要件である | 記述あり |
| U-C4 ランチャーの関係／秘匿退行 | 「§5-6 は『要点のみ』のスニペットであり、既存ランチャーを置き換える意図はない」→ ファイル名が完全に一致し、§7-3 手順5 が「配置して commit」と指示している以上、読者は置き換えと読む。かつ秘匿値の受け皿が消えている点は要点版であっても看過できない | 記述あり |
| U-I2 deny 設定の指示欠落 | 「deny の具体パターンは CLI の版差で陳腐化するので載せないほうがよい」→ 本書は他の箇所で版依存の事実を多数載せており（付録A・付録B）、deny だけ避ける理由はない。§8-1 の版依存注記でカバーできる | 記述あり |
| U-I4 未 clone 状態 | 「ランチャー経由の起動を周知するのだから初回も問題ない」→ 周知は強制力を持たず、§6-1 は「素の `claude` でも効く」を優位点として掲げている。優位点として主張する以上、成立条件を書く必要がある | 記述あり |

---

## 補足（自己検証メモ）

* 下流成果物との整合に関する指摘（U-C1〜U-C4・U-I1）は、すべて該当ファイルを実際に開いて確認している。推測で書いた箇所は無い。
* U-S4 のみ、git の挙動を再実測せず文書化された挙動に基づいて指摘しているため、**推測を含む**旨を本文に明記した。**なお Round 2 で、レビューイが別途この挙動を実測し、`switch` 失敗後に `reset --hard` が現在ブランチを上書きすることを確認している**（実用性観点 C-1 参照）。
* 「案C-1 という方針の是非」「rebase を選んだことの是非」「v1.2 を残すこと自体の是非」「本書内に情報を保持する執筆方針の是非」は作業指示者決定事項として指摘対象から除外した。
* 出典番号 `[1]`〜`[41]` の定義は全件存在し欠番は無いことを確認した（`[36]` のみ表中の並び順が `[41]` の後になっているが実害が無いため指摘していない）。

---

## 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 0.8 | 2026-09-14 | 初版作成（Claude Opus 5・作業指示者代理・Round 1） |
| 0.9 | 2026-09-14 | レビューイ FB（Round 2）＋ レビューア最終所見・修正案反映完了（Round 3）。取り下げ 0 件・重要度変更 0 件。U-C3／U-C4 は既存配布キット 3 リポとランチャーの去就という作業指示者判断事項を含むため、本書側は「推奨の提示」までとし確定は 1.0 版へ持ち越す方針で確定 |
| 1.0 | | |
